# Roadmap — Ahorita (Cuenca Viva)

Ver `VISION_MAESTRA.md` (máxima autoridad conceptual del proyecto, adoptada el 2026-07-21), `PRODUCT_MANIFESTO.md` (identidad, principios y corazón del producto, subordinado a la Visión Maestra), `PRODUCT_STRATEGY.md` (cómo se materializa esa identidad 2026-2028, subordinado al Manifesto), `ARCHITECTURE.md` para la visión y arquitectura técnica del ecosistema, `AI_PHILOSOPHY.md` para la filosofía y el comportamiento de la Guía IA, `MASTERPLAN.md` para el plan maestro de desarrollo detallado, `PROJECT.md` para el detalle técnico de cada fase ya implementada, y `CHANGELOG.md` para el registro de cambios archivo por archivo. Este documento es el mapa de alto nivel: qué está terminado y qué sigue.

## Fases completas

1. **Base del proyecto** — estructura de carpetas, sistema de diseño (Fraunces + Inter, tokens de color/tipografía/espaciado), cliente Supabase, login/registro con modo invitado y registro progresivo.
2. **Lugares** — cuadrícula filtrable por zona (estilo Instagram Explore), muro de lugar como Bottom Sheet, preguntas/respuestas/estados con like y gate de autenticación.
3. **Descubrimiento** — stories por canal, filtro de canal/categoría.
4. **Guía IA** — Edge Function de Supabase que llama a la API de Claude con contexto real de la base de datos (nunca la API key en el cliente); IA contextual dentro de cada lugar.
5. **Pulido y lanzamiento inicial** — perfil de usuario (lugares guardados, seguir personas), registro de negocios con estado "en revisión", panel de administración simple, PWA instalable.
6. **Rediseño premium de "Inicio"** — Inicio pasa a ser un feed exclusivo de eventos (no una mezcla de todo); mapa real en Explorar (Leaflet + CARTO + geolocalización); "Cómo llegar" con distancia real y deep links; notificaciones push reales (Web Push); sistema de diseño editorial (ritmo del feed, tipografía Fraunces con eje óptico, duotono cálido en fotos, transición continua feed→detalle); Guía IA como cápsula editorial; coherencia semántica de fotografía por categoría con fallback de marca; "Selección del editor" con curaduría manual real (`editor_pick`).
7. **✅ Administración completa de eventos y lugares** (cerrada el 2026-07-17) — CRUD completo desde `/admin/eventos` y `/admin/lugares` sin tocar código ni base de datos manualmente: crear, editar, duplicar, ocultar/publicar, eliminar; editor progresivo de 7 secciones con vista previa real; ciclo de vida completo de estados (borrador/publicado/oculto/finalizado/cancelado en eventos; borrador/publicado/oculto en lugares) con publicación programada y expiración; estados de guardado visibles y confirmación antes de salir con cambios sin guardar; menú de acciones y metadatos (estado/actualización/autor) por elemento; selector de ubicación con búsqueda de dirección. Ver el checklist completo de funcionalidades, limitaciones conocidas y el desglose de qué datos son reales/de prueba/fallback en `PROJECT.md`, sección "FASE CERRADA — Administración de eventos y lugares". Checkpoint de Git: tag `checkpoint-admin-eventos-lugares`.
8. **✅ Fase 1 del ecosistema social — Modelo de datos fundacional** (cerrada el 2026-07-17) — unificación del modelo de datos en cinco bloques independientes, cada uno propuesto, implementado y verificado contra Postgres real por separado: **Bloque 1** (esquema fundacional: `cities`, `zones`, `actors`, `interactions`, `event_details`, `consent_records`, invisibles para el usuario); **Bloque 2** (identidad: `profiles`/`businesses` vinculados a `actors`); **Bloque 3** (contenido: `event_details` poblada, primer caso del patrón núcleo+detalle); **Bloque 4** (interacciones y territorio: `post_likes`/`saved_places`/`saved_events`/`follows` → `interactions`, `places.area` → `places.zone_id`); **Bloque 5** (privacidad: consentimiento versionado, `data_requests`, anonimización de contenido colaborativo, negocios sin propietaria, Edge Functions de exportación/eliminación). Completa en arquitectura, migraciones, restricciones, RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria contra un proyecto Supabase real (ver `PROJECT.md`, sección "FASE 1 CERRADA"). Checkpoint de Git: tag `checkpoint-fase1-ecosistema-social`.
9. **✅ Fase 2 — Verificación robusta y roles granulares** (cerrada el 2026-07-18) — dos bloques, cada uno propuesto, implementado y verificado contra Postgres real por separado: **Bloque A** (esquema: `roles`/`actor_roles` many-to-many sobre Actor, `verifications` asociada a Actor no a `businesses`, `role_audit_log`, prevención de auto-aprobación); **Bloque B** (ciclo de vida completo: solicitud/evidencia/revisión/aprobación con snapshot automático, vigencia anual con 30 días de gracia, vencimiento automático, revocación, `verification_status_log`/`verification_notices`/`evidence_access_log`, bucket privado de Storage `verification_evidence`, Edge Function `process-verification-lifecycle`) — corrigió además, durante la propia implementación, un defecto real de pertenencia de Actor heredado del Bloque A (nunca llegó a producción). Completa en arquitectura, migraciones, RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria contra un proyecto Supabase real (ver `PROJECT.md`, sección "FASE 2 CERRADA"). Checkpoint de Git: tag `checkpoint-fase2-verificacion-roles`.
10. **✅ Fase 3 — Identidad social plena** (cerrada funcionalmente el 2026-07-18) — tres bloques, cada uno propuesto, implementado y verificado contra Postgres real por separado: **Bloque A** (perfil unificado y administración: `actor_profile_details`, `actor_managers`, `actor_editable_by_current_user`); **Bloque B** (horarios, catálogo y ubicación estructurada); **Bloque C** (perfiles visibles y editables — "Centro del Negocio", 7 entregas): Entrega 1 (perfil público unificado), Entrega 2 (actividad/acciones/catálogo/eventos/galería/Guía IA/Acerca de), Entrega 3 (edición de logo/portada/bio/galería), Entrega 4 (edición de horarios y catálogo), Entrega 5 (selector de perfil unificado, retiro de `ProfilePage.jsx`, nueva ruta `/ajustes`), Entrega 6 (consolidación: seguimiento unificado bajo `interactions` para personas y negocios, `follows` retirado del frontend como legacy de solo respaldo, autointeracción bloqueada en la base de datos), Entrega 7 (búsqueda y descubrimiento de negocios: `/buscar` extendida con `search_actors`, Actor-céntrica, categorías rápidas). El Bloque C se cerró en 7 entregas, sin Entrega 8: ver en `PROJECT.md`, sección "FASE 3 CERRADA", el análisis transparente de por qué no correspondía extender artificialmente el bloque. Completa en arquitectura, migraciones, RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria (ver más abajo y el detalle en `PROJECT.md`, sección "FASE 3 CERRADA"). Checkpoint de Git: tag `checkpoint-fase3-identidad-social`.
11. **✅ Fase 4 — Contenido social ampliado, el Feed como centro** (cerrada el 2026-07-19) — rediseñada tres veces bajo `PRODUCT_MANIFESTO.md`/`PRODUCT_STRATEGY.md` antes de implementarse (ver `FASE4_CONTRATO_ARQUITECTONICO.md`), ordenada alrededor del Feed en vez de la base de datos, cuatro bloques cada uno propuesto, implementado, verificado y aprobado por separado: **Bloque 1** (el Feed como contrato de composición multi-fuente, probado con Eventos sin modificarlos); **Bloque 2** (Publicaciones alimentan el Feed, cinco ajustes de autenticidad); **Bloque 3** (Promociones alimentan el Feed, siete ajustes de producto, corrige un hallazgo de seguridad heredado del Bloque 2); **Bloque 4** (Compartidos fortalecen el Feed, un único hook `useShareContent` para las cinco superficies, sin ninguna migración nueva, corrige después un hallazgo de layout heredado del Bloque 2 mediante `ContentActionsRow`). Principio rector sostenido sin excepciones: `events` nunca se tocó, verificado explícitamente en cada bloque. Completa en arquitectura, migraciones (`0030`, `0031`), RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria (ver más abajo y el detalle completo en `PROJECT.md`, sección "FASE 4 CERRADA"). Checkpoint de Git: tag `checkpoint-fase4-feed-social`.
12. **✅ Fase 5B — Interacción social plena** (cerrada el 2026-07-22) — primera fase construida enteramente bajo la Metodología Permanente de Trabajo adoptada tras `VISION_MAESTRA.md`, tres bloques cada uno propuesto, implementado, verificado y aprobado por separado: **Bloque 1** (cambio de fuente de verdad: "me gusta"/"guardado" de Eventos y Lugares migran de `post_likes`/`saved_events`/`saved_places` a `interactions`, reconciliación bidireccional, migración `0032`); **Bloque 2** (reacciones "Quiero ir"/"Ya fui" exclusivas de Eventos, compuerta temporal reforzada en la base de datos, migración `0033`); **Bloque 3** (comentarios generalizados a Evento y Publicación —Promoción explícitamente excluida—, actor de sistema "Cuenta eliminada" para anonimización compartida, índice único parcial sobre `interactions`, soft-delete de dos estados permanente, migración `0034`). Los tres bloques encontraron y corrigieron siete defectos reales durante su propia verificación contra Postgres 16 real, ninguno llegado a un commit sin corregir — el nivel de rigor más alto alcanzado por el proyecto hasta ahora. Completa en arquitectura, migraciones (`0032`-`0034`), RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria (ver más abajo y el detalle completo en `PROJECT.md`, sección "FASE 5B CERRADA"). Checkpoint de Git: tag `checkpoint-fase5b-interaccion-social`.
13. **✅ Fase 6 — Descubrimiento inteligente v2** (cerrada el 2026-07-23) — reemplaza el feed "solo proximidad temporal" por una composición de seis entradas co-iguales, **nunca una fórmula de puntuación**, construida bajo triple autoridad documental (`FASE6_FILOSOFIA_DESCUBRIMIENTO.md`, `FASE6_CONTRATO_ARQUITECTONICO.md`, `VISION_MAESTRA.md`) en cuatro bloques, cada uno propuesto, implementado y verificado por separado: **Bloque 1** (Motor de Afinidad: perfil legible y corregible por la propia persona, decaimiento con piso, privacidad estricta sin excepciones, migraciones `0035`-`0037`); **Bloque 2** (Motor de Garantías: novedad/diversidad/equidad/serendipia, ninguna llega nunca a cero, migraciones `0038`-`0039`); **Bloque 3** (Motor Editorial: reemplaza `editor_pick` por `editorial_selections` con autoría y ciclo de vida, migración `0040`); **Bloque 4** (Compositor del Feed: fusiona las seis entradas mediante colas justas ponderadas —weighted fair queuing—, anti-monopolio transversal, migración `0041`). Los cuatro bloques encontraron y corrigieron ocho defectos reales durante su propia verificación contra Postgres 16 real, incluyendo un rendimiento verificado objetivamente con `EXPLAIN ANALYZE` (663ms → 115ms). El cierre formal de la fase corrigió además dos documentos (`MASTERPLAN.md`, `ARCHITECTURE.md`) que todavía describían el plan original de "feed híbrido" ya reemplazado antes de implementarse. Completa en arquitectura, migraciones (`0035`-`0041`), RLS, lógica PostgreSQL, documentación y pruebas locales — **no lista para producción** hasta validar la deuda técnica obligatoria (ver más abajo y el detalle completo en `PROJECT.md`, sección "FASE 6 CERRADA"). Checkpoint de Git: tag `checkpoint-fase6-descubrimiento-inteligente`.

## Fases futuras del `MASTERPLAN.md` (pendientes de definición y aprobación)

`ARCHITECTURE.md` (visión), `AI_PHILOSOPHY.md` (filosofía y comportamiento de la Guía IA) y `MASTERPLAN.md` (plan de desarrollo, con dependencias, tablas, riesgos, criterio de terminado y aporte a la Guía IA por fase) ya están aprobados, ahora subordinados en identidad y prioridad a `VISION_MAESTRA.md` y, bajo ella, a `PRODUCT_MANIFESTO.md`/`PRODUCT_STRATEGY.md`. El Product Owner además cerró once decisiones estratégicas pendientes (vigencia de verificación sin cobro automático, catálogo final de reacciones, techo de contenido patrocinado del 15% auditable, protección explícita de que el contenido pagado nunca compra una recomendación de la Guía IA, alcance simple de Historias, proveedores de pagos/transporte abiertos a investigación, pricing de suscripción diferido, privacidad adelantada a la Fase 1, y división de la fase de interacción social — ver el registro completo en `MASTERPLAN.md`). **Principio rector aprobado: la IA es un eje transversal de todo el producto, no una fase aislada** — cada fase documenta explícitamente qué le aporta a la Guía IA, aunque su integración técnica definitiva llegue después.

Resumen de las fases (detalle completo en `MASTERPLAN.md`; ver `PRODUCT_MANIFESTO.md` §11 para la revisión de prioridades):

1. ✅ Unificación del modelo de datos fundacional (Actor, Publicación, Interacción, Ciudad) + línea base de privacidad y consentimiento — **cerrada**.
2. ✅ Verificación robusta de negocios (vigencia anual sin cobro automático) + roles granulares — **cerrada**.
3. ✅ Identidad social plena (perfiles y negocio extendido) — **cerrada funcionalmente**, tres bloques (A, B y C con 7 entregas). Ver `PROJECT.md`, sección "FASE 3 CERRADA", para el detalle completo.
4. ✅ Contenido social ampliado (Publicaciones, Promociones y Compartidos), rediseñada como "Feed como centro" — **cerrada**, cuatro bloques. Ver `PROJECT.md`, sección "FASE 4 CERRADA", para el detalle completo.
5B. ✅ Interacción social plena (comentarios/guardados/reacciones — catálogo cerrado: Me gusta, Quiero ir, Ya fui) — **cerrada**, tres bloques. Ver `PROJECT.md`, sección "FASE 5B CERRADA", para el detalle completo.
6. ✅ Descubrimiento inteligente v2 (Motor de Afinidad, Motor de Garantías, Motor Editorial, Compositor del Feed — sin fórmula de puntuación, techo de contenido patrocinado del 15% reservado para la Fase 11) — **cerrada**, cuatro bloques. Ver `PROJECT.md`, sección "FASE 6 CERRADA", para el detalle completo.
7. Guía IA v2 (sesiones persistentes y personalización)
8. Historias (máximo 30 segundos, alcance simple) y video como contenido de primera clase
9. QR y experiencias físicas (check-in, promociones, validación de entradas, turismo)
10. Comercio: reservas y venta de entradas internas (proveedor de pagos aún por definir)
11. Monetización activa y publicidad (pricing de suscripción aún por definir)
12. Integración de movilidad — ventana condicionada a verificar formalmente el mecanismo real del proveedor antes de implementar
13. Madurez operativa y cumplimiento (moderación, soporte, i18n, legal, feature flags, observabilidad — la privacidad ya no se introduce aquí, se adelantó a la Fase 1)

**Ninguna fase se implementa hasta aprobación explícita.** Antes de la Fase 1 específicamente, falta presentar y aprobar una propuesta concreta de ejecución (alcance exacto, estrategia de migración, compatibilidad, reversión, riesgos, pruebas, criterios de aceptación, entregables y estimación) — recién con esa propuesta aprobada empieza la implementación.

## Deuda técnica obligatoria antes de producción (acumulada, ver detalle en `PROJECT.md`)

- **Fases 1-4 y 5B completas**: validar contra un proyecto Supabase real desplegado (Edge Functions, Storage, triggers programados) — nunca verificado más allá de Postgres local. En la Fase 5B afecta en particular la verificación completa de `process-account-deletions` (reasignación de comentarios a "Cuenta eliminada").
- **Fase 4**: canje físico validado de Promoción reservado a propósito para la Fase 9 (una Promoción de esta fase es un beneficio declarado, no un cupón validado en el punto de venta); concurrencia real del `unique` de `interactions` bajo el nuevo caso de uso de Compartir, mismo límite ya documentado para me gusta/guardar desde la Fase 3, ahora extendido; consolidación de `events` sobre el núcleo compartido de Publicación como punto de decisión futuro explícito, no resuelto en esta fase (ver `PROJECT.md`, sección "FASE 4 CERRADA").
- **Fase 5B**: `post_likes` permanece parcialmente legacy (activa solo para Comunidad, retiro condicionado a que Comunidad migre a `interactions` en una fase futura); `saved_events`/`saved_places`/`event_comments` permanecen como legacy de solo respaldo, sin retiro definitivo todavía; sin interfaz de moderación de comentarios en `/admin`, aunque la base de datos ya lo permite a nivel de admin (ver `PROJECT.md`, sección "FASE 5B CERRADA").
- **Bloque C, Entrega 1-7 (Fase 3)**: sin prueba end-to-end contra Auth+PostgREST+RLS realmente desplegados (mismo motivo que arriba); sin interfaz para gestionar `actor_managers` (invitar/revocar administradores operativos, detectado en la Entrega 5) — **registrada como obligatoria antes de que los negocios puedan publicar y operar plenamente en la fase social**, debe contemplar invitar, aceptar/rechazar, impedir invitaciones duplicadas, mostrar propietarios/administradores activos, revocar, registrar quién invitó/revocó, impedir que un administrador operativo invite o revoque a otros, y retirar permisos de inmediato al revocar (ver aprobación de la Entrega 6 en `PROJECT.md`).
- **Gestión de archivos huérfanos en Storage** (detectada en la Entrega 3, ver `PROJECT.md`): antes de producción debe existir un mecanismo seguro que (1) elimine el objeto anterior al reemplazar una imagen, (2) elimine el objeto al borrar una imagen, (3) confirme que el archivo pertenece realmente al actor que solicita eliminarlo, (4) evite borrar archivos todavía referenciados desde otra parte de la aplicación, (5) registre fallos de eliminación, (6) permita limpieza periódica de objetos sin referencia, (7) respete los procesos de eliminación de cuenta y privacidad de la Fase 1, Bloque 5. No se corrige de forma aislada dentro de una entrega — requiere su propio diseño y aprobación explícita.
- **`replaceBusinessHours` no es atómico** (detectado en la Entrega 4): borra e inserta en dos llamadas REST separadas; riesgo bajo pero documentado.
- **`follows` (Fase 1) permanece como estructura legacy de solo respaldo** (detectado en la Entrega 6): el frontend ya no la usa, pero la tabla y sus filas no se retiran todavía — el retiro definitivo requiere su propia migración futura, después de un periodo de convivencia observado en producción.
- **Sin pantalla de "negocios guardados"** (detectado en la Entrega 6): `guardar` un negocio funciona, pero no hay ningún lugar de la interfaz para ver esa lista — funcionalidad futura de producto, no deuda crítica.
- **Concurrencia real de la restricción `unique` de `interactions`** (Entrega 6): aproximada con inserción duplicada secuencial y doble-clic en Playwright, no probada con múltiples conexiones simultáneas genuinas — requiere un proyecto desplegado.
- **Sin autocompletado ni ranking en la búsqueda** (Entrega 7): reservado explícitamente para la Fase 6 ("Descubrimiento inteligente v2"), no una omisión.
- **Sin búsqueda de personas** (Entrega 7): el índice ya cubre actores tipo persona técnicamente, pero exponerla abre una pregunta de privacidad/descubribilidad nunca aprobada.
- **Negocios no aparecen en el Mapa/Explorar** (Entrega 7): `ExplorePage` sigue trabajando solo sobre `places` — extensión futura razonable, no construida.
- **Fase 6**: `events.editor_pick` permanece en el esquema como fuente histórica del backfill hacia `editorial_selections`, sin nuevas escrituras; `discoverable_content()` se evalúa de forma independiente dentro de cada uno de los cinco carriles que la consumen en vez de una sola vez compartida (aceptable al volumen actual); sin verificación Playwright real desde el Bloque 3 en adelante, por ausencia de Supabase local inicializado en este entorno (ver `PROJECT.md`, sección "FASE 6 CERRADA").

## Explícitamente fuera de alcance por ahora (recordatorio recurrente)

Reels, nuevos bloques editoriales más allá de "Selección del editor". Monetización y la integración de movilidad ya tienen fase asignada en `MASTERPLAN.md` (Fases 11 y 12) pero siguen sin implementarse — la de movilidad además condicionada a verificar el mecanismo real del proveedor antes de construir nada.
