# Base de datos — Ahorita

## Cómo aplicar el esquema

1. Crea un proyecto en [supabase.com](https://supabase.com).
2. Ve a **SQL Editor** en el dashboard del proyecto.
3. Pega el contenido de `migrations/0001_init.sql` y ejecútalo.
4. Copia `Project Settings > API > Project URL` y `anon public key` a tu archivo `.env` (ver `.env.example` en la raíz).

Si prefieres la CLI de Supabase:

```bash
supabase link --project-ref <tu-project-ref>
supabase db push
```

## Guía IA (Edge Function)

La app llama a la API de Claude solo desde `supabase/functions/ai-guide`, nunca directo desde el navegador, para no exponer la API key.

```bash
supabase secrets set ANTHROPIC_API_KEY=tu-api-key-de-anthropic
supabase functions deploy ai-guide
```

La función usa `claude-sonnet-5`. Recibe `{ messages, placeId? }`: sin `placeId` arma contexto de toda la ciudad (lugares + eventos próximos); con `placeId` arma contexto de ese lugar específico (preguntas, respuestas, estados recientes y lugares cercanos en la misma zona).

## Notificaciones push (Edge Function)

Genera tu propio par de claves VAPID (no requiere cuenta externa, es criptografía local):

```bash
npx web-push generate-vapid-keys
```

Pega la **Public Key** en `VITE_VAPID_PUBLIC_KEY` (`.env` del frontend) y configura ambas claves como secrets de la función:

```bash
supabase secrets set VAPID_PUBLIC_KEY=tu-public-key
supabase secrets set VAPID_PRIVATE_KEY=tu-private-key
supabase secrets set VAPID_SUBJECT=mailto:tu-correo@ejemplo.com
supabase functions deploy send-push
```

`send-push` recibe `{ userId, title, body?, url? }` y le manda la notificación a todas las suscripciones guardadas de ese usuario (tabla `push_subscriptions`). Por ahora se dispara desde el cliente cuando alguien responde una pregunta — para producción, lo más robusto es moverlo a un [Database Webhook](https://supabase.com/docs/guides/database/webhooks) que llame a la función directamente al insertar en `answers`, en vez de depender del cliente.

## Privacidad: exportación y eliminación de cuenta (Edge Functions)

Parte del mecanismo del Bloque 5 (ver `PROJECT.md`). **Nota de verificación**: a diferencia de toda migración SQL de este proyecto, estas dos funciones no pudieron probarse contra un proyecto Supabase real ni contra su API de administración de Auth en este entorno de desarrollo — están escritas y revisadas cuidadosamente siguiendo la misma convención que `ai-guide`/`send-push`, pero requieren una verificación end-to-end contra un proyecto real antes de confiar en ellas en producción.

```bash
supabase functions deploy export-user-data
supabase secrets set CRON_SECRET=un-secreto-largo-y-aleatorio
supabase functions deploy process-account-deletions
```

- `export-user-data` la llama el propio usuario autenticado (nunca acepta un `userId` de parámetro): deriva quién es del token de su sesión (`auth.getUser()`) y devuelve en JSON exactamente sus propios datos — perfil, negocios, preguntas/respuestas/estados/comentarios, eventos/lugares creados, guardados, follows, likes, su historial de `consent_records`/`data_requests`, y (agregado en la Fase 7) la conversación activa de la Guía IA, sus turnos, el Conocimiento Permanente vigente con su trazabilidad legítima (nunca el registro crudo), y el perfil agregado de Afinidad (`affinity_profile()`, nunca `affinity_contributions` crudas ni valores ya borrados).
- `process-account-deletions` NO la debe llamar un usuario final — exige un header `x-cron-secret` que coincida con el secret `CRON_SECRET`, y está pensada para invocarse periódicamente desde un disparador externo (no hay `pg_cron` configurado todavía). Reclama de forma atómica (pasando su estado de `pendiente` a `en_proceso` en una sola sentencia, cierre de la Fase 7) toda solicitud de `data_requests` tipo `eliminacion` cuyo periodo de gracia de 30 días ya venció -- ese reclamo es la frontera exacta después de la cual una cancelación de la persona ya no tiene efecto. Para cada una, reconoce con seguridad si la cuenta ya no existe (un ciclo anterior la eliminó parcialmente) para nunca repetir trabajo destructivo ni quedar atrapada sin alcanzar un estado terminal; en caso contrario, llama a `auth.admin.deleteUser`, lo que en un solo paso invalida las sesiones activas de esa persona, revoca sus tokens, y dispara todos los `on delete cascade`/`on delete set null` ya definidos en el esquema (ver el detalle completo en `PROJECT.md`).

## Verificaciones: ciclo de vida (Edge Function)

Parte del mecanismo del Bloque B de la Fase 2 (ver `PROJECT.md`). Misma nota de verificación que las de privacidad: no se pudo probar contra un proyecto Supabase real en este entorno.

```bash
supabase functions deploy process-verification-lifecycle
```

- `process-verification-lifecycle` NO la debe llamar un usuario final — mismo secreto `CRON_SECRET` que `process-account-deletions` (comparten solo la credencial de invocación, nunca código ni lógica). Dos responsabilidades independientes en la misma función porque comparten los mismos datos de entrada: (1) envía avisos (30/15/7 días antes, día del vencimiento, fin de gracia) reutilizando `send-push` en vez de duplicar su lógica, con `verification_notices` garantizando que ningún aviso se reenvíe; (2) marca `vencido` cada `verifications` `aprobado` cuyo periodo de gracia de 30 días ya terminó — la única vía por la que `vencido` puede alcanzarse, ya que ningún admin humano puede hacerlo vía RLS normal.

## Notas sobre el esquema

- `profiles` extiende `auth.users` (Supabase Auth ya maneja email/contraseña y OAuth). Un trigger crea el perfil automáticamente al registrarse.
- Todas las tablas tienen Row Level Security activado. Lectura pública en la mayoría de tablas (contenido de la red social es abierto); escritura restringida al autor autenticado.
- `businesses.status` empieza en `'pendiente'` — solo se muestran públicamente cuando un admin lo cambia a `'aprobado'` (por ahora, manual desde el dashboard de Supabase; un panel de admin vendrá en una fase posterior).
- `answers.verified` — por ahora sin política de escritura para usuarios normales; solo el rol de servicio (admin) debería poder marcarlo hasta definir un sistema de reputación comunitario.
- `channels` viene pre-poblado con los 10 canales del sistema de diseño.
- `0002_storage.sql` crea el bucket público `media` (fotos/videos de estados) con lectura pública y escritura solo para usuarios autenticados.
- `0003_seed_places.sql` precarga ~10 lugares reales del Centro Histórico de Cuenca para evitar el "arranque en frío". Las imágenes son placeholders (`picsum.photos`) — reemplázalas por fotos reales antes de lanzar.
- `0004_seed_editorial.sql` precarga una tarjeta editorial de ejemplo — corre igual que las demás en orden, pero `0010_events.sql` elimina esa tabla después (ver más abajo). Se conserva el archivo por historial.
- `0005_profile_social.sql` agrega `saved_places` (lugares guardados), `follows` (seguir personas), y `profiles.is_admin` con las políticas necesarias para el panel de administración (`/admin`).
- `0006_author_profile_relations.sql` reapunta los FK de autor de `questions`/`answers`/`statuses` a `profiles` en vez de `auth.users`, para poder mostrar el nombre de usuario y el botón de "seguir".
- `0007_feed_and_likes.sql` agrega `places.tag/hours/website/tickets_url/menu_url/description` (para las tarjetas del feed de Inicio) y `post_likes` (like genérico para cualquier tipo de contenido, con la vista `post_like_counts` para el conteo agregado).
- `0008_seed_tags.sql` etiqueta algunos lugares semilla (`imperdible`, `gratis`, `hoy`) para que el feed de Inicio no se vea plano en el arranque en frío.
- `0009_push_subscriptions.sql` agrega `push_subscriptions` (una fila por dispositivo suscrito a notificaciones push).
- `0010_events.sql` — **corrección de producto**: Inicio pasa a ser un feed solo de eventos. Elimina `editorial_posts` y agrega `events` (con `likes_count`/`comments_count` desnormalizados, mantenidos por trigger), `event_comments` y `saved_events`. Solo administradores o el dueño de un negocio ya aprobado pueden crear eventos.
- `0012_seed_events.sql` precarga 4 eventos de ejemplo (festival, concierto, feria gastronómica, carrera) con fechas relativas a "ahora" para que siempre aparezcan como próximos.
- `0013_admin_lifecycle.sql` — administración completa de eventos y lugares (`/admin/eventos`, `/admin/lugares`): agrega `events.status/organizer/publish_at/expires_at` y `places.status`, políticas UPDATE/DELETE para `places` (antes solo tenía INSERT admin), y ajusta el SELECT público de ambas tablas a `status = 'publicado' OR is_admin()`.
- `0014_updated_at_and_creator.sql` — agrega `updated_at` (mantenido por trigger `set_updated_at`) a `events`/`places`, y `created_by` a `places` (`events` ya lo tenía), para mostrar "actualizado hace…" y autor en los listados de `/admin`.
- `0015_bloque1_esquema_fundacional.sql` — **Fase 1 del ecosistema social (ver `MASTERPLAN.md`), Bloque 1: esquema.** Agrega únicamente tablas nuevas, sin tocar ni una fila de las tablas existentes: `cities` (poblada con "Cuenca"), `zones` (hija de `cities`, con jerarquía de un solo nivel; poblada con "Centro Histórico" y "Turi", las únicas zonas realmente en uso hoy en `places.area`), `actors` (identidad unificada persona/negocio/organizador/sistema; ya incluye los actores de sistema "Guía IA" y "Ahorita Editorial"), `interactions` (polimórfica, catálogo: me_gusta/quiero_ir/ya_fui/guardado/seguimiento/compartir — "reportar" queda deliberadamente fuera, es su propia entidad de moderación en una fase posterior), `event_details` (primer caso del patrón "núcleo genérico + tabla de detalle", nace vacía — la separación real de los datos de `events` ocurre en el Bloque 3), y `consent_records` (línea base de privacidad, adelantada desde la fase final del plan original). Ninguna de estas tablas está todavía conectada a la aplicación — es deliberadamente invisible para el usuario final hasta los bloques siguientes.
- `0016_bloque2_identidad.sql` — **Fase 1, Bloque 2: identidad.** Vincula `profiles` y `businesses` con `actors`, sin alterar ninguna columna de ninguna de las dos: crea un actor tipo `persona` por cada `profile` existente y uno tipo `negocio` por cada `business` existente (backfill, protegido con `where not exists` además de la restricción `unique` ya existente), y agrega dos triggers (`on_profile_created_actor`, `on_business_created_actor`, ambos `security definer` con el mismo patrón que `handle_new_user`) para que todo registro o negocio nuevo cree su actor automáticamente de ahora en adelante — sin esto, la correspondencia solo sería cierta en el instante de la migración. El `display_name` de un actor persona/negocio es una foto del momento de creación (usa `username`/`name` en ese instante); no se mantiene sincronizado ante ediciones posteriores — ver la nota de limitación conocida en `PROJECT.md`.
- `0017_bloque3_contenido.sql` — **Fase 1, Bloque 3: contenido.** Puebla `event_details` (creada vacía en el Bloque 1) con una copia fiel de `start_at/end_at/price/ticket_url/organizer` de cada evento ya existente (backfill, protegido con `where not exists`) — primer caso real de datos fluyendo por el patrón "núcleo genérico + tabla de detalle" de Publicación. La separación es deliberadamente **aditiva**: ninguna columna se retira de `events` en este bloque, y `event_details` es una **fotografía** tomada en este momento, sin trigger de sincronización posterior (mismo criterio que `actors.display_name` en el Bloque 2) — ver el detalle de las dos decisiones arquitectónicas evaluadas y aprobadas en `PROJECT.md`.
- `0018_bloque4_interacciones_territorio.sql` — **Fase 1, Bloque 4: interacciones y territorio.** Puebla `interactions` con una copia fiel de `post_likes` (→ `me_gusta`), `saved_places`/`saved_events` (→ `guardado`) y `follows` (→ `seguimiento`, resuelto vía `actors.profile_id`), y agrega `places.zone_id` mapeado desde `places.area` únicamente cuando coincide exactamente con una `zones.name` existente (nunca se inventa ni corrige una zona ambigua). Reemplaza la política de lectura pública de `interactions` (creada en el Bloque 1) por una que excluye el tipo `guardado` — un guardado solo es visible para su dueño o un administrador, igual privacidad que ya tenían `saved_places`/`saved_events`. Aditivo y sin sincronización en vivo, mismo criterio que los Bloques 2 y 3 — ver el análisis previo aprobado y la verificación completa en `PROJECT.md`.
- `0019_bloque5_privacidad.sql` — **Fase 1, Bloque 5: privacidad.** A diferencia de los Bloques 1-4 (deliberadamente invisibles), este activa comportamiento real: (1) `consent_records` gana `consent_key`/`document_version` (consentimiento versionado, retirable sin borrar el historial) y pierde su FK hacia `auth.users` para sobrevivir a la eliminación de la cuenta que describe; (2) `data_requests` (tabla nueva) separa el flujo de trabajo de una solicitud de exportación/eliminación del log inmutable de consentimiento; (3) `questions`/`answers`/`statuses`/`event_comments.author_id` pasan de `on delete cascade` a `on delete set null` — el contenido colaborativo sobrevive (anonimizado) a que su autor elimine su cuenta, en vez de arrastrar consigo las respuestas de terceros; (4) `businesses.owner_id` pasa a `on delete set null`, con un trigger (`handle_orphaned_business`) que fuerza `status = 'sin_propietario'` cuando el dueño desaparece — el negocio deja de ser público pero conserva su historial hasta que un admin lo reasigne (se amplía además la política de lectura de `businesses` para que los admins puedan verlo). Ver el análisis previo, las cuatro bifurcaciones de producto resueltas, y el detalle completo de qué queda fuera de esta migración (invalidación de sesiones/tokens, QR, borrado de archivos en Storage) en `PROJECT.md`.
- `0020_fase2_bloqueA_verificacion_roles.sql` — **Fase 2, Bloque A: esquema de verificación y roles.** Aditivo, sin tocar `profiles.is_admin` ni ninguna política existente: agrega `roles` (catálogo libre, agregar un rol nuevo es un `INSERT`), `actor_roles` (asignación many-to-many sobre `actors`, nunca se borra una fila — se revoca con `revoked_at`), `verifications` (asociada a `actors`, no a `businesses`, para poder verificar en el futuro distintos tipos de entidad sin rediseñar; la evidencia se referencia — `evidence_ref` — nunca se guarda en la tabla), y `role_audit_log` (poblada solo por trigger, nunca por la aplicación). Dos triggers impiden asignar un rol o una verificación a un actor tipo `sistema`. La política de `verifications` impide explícitamente que un actor apruebe/rechace/revoque su propia verificación, incluso siendo administrador. Backfill: cada `profile.is_admin=true` recibe el rol `administrador` (`origin='migracion'`); cada `business.status='aprobado'` recibe una `verification` `aprobado` con un año de vigencia desde el momento de la migración (`origin='migracion'`). Ver el análisis técnico completo (comparación de alternativas de modelo de roles, matriz de permisos, justificación de Actor sobre Business para verificación) en `PROJECT.md`.
- `0021_fase2_bloqueB_ciclo_vida_verificaciones.sql` — **Fase 2, Bloque B: ciclo de vida, vigencia y renovación.** Corrige un defecto real encontrado en el Bloque A: las políticas de "pertenencia" comparaban solo `actors.profile_id`, lo que nunca podía ser cierto para un actor `negocio`/`organizador` (siempre `profile_id = null`) — bloqueaba el caso central de este bloque (el dueño de un negocio verificando su propio negocio). Se agrega `public.actor_belongs_to_current_user(actor_id)` (`security definer`, cubre persona vía `profile_id` y negocio/organizador vía `businesses.owner_id`) y se reemplaza en las políticas afectadas de `verifications`/`actor_roles`. Agrega columnas (`revoked_by`/`revoked_at`, `snapshot_owner_id`/`snapshot_category`/`snapshot_lat`/`snapshot_lng`, pobladas automáticamente al aprobar), `verification_status_log`/`verification_notices`/`evidence_access_log` (auditoría, poblada por trigger o por la Edge Function), y un trigger de protección de campos que congela cualquier columna sensible si quien actualiza no es admin ni el proceso automatizado — con una guarda explícita adicional contra la auto-aprobación, necesaria porque en Postgres las cláusulas `WITH CHECK` de todas las políticas permisivas de `UPDATE` se combinan con `OR`, sin importar cuál política autorizó el `USING`. `vencido` solo lo alcanza el proceso automatizado (nunca RLS normal); periodo de gracia de 30 días durante el cual `aprobado` se conserva. Ver el análisis completo y los dos defectos encontrados durante la implementación en `PROJECT.md`.
- `0022_fase2_bloqueB_storage_evidencias.sql` — bucket privado `verification_evidence` (PDF/JPG/PNG, 10 MB máx.), un único archivo por solicitud vía ruta fija `<verification_id>/evidence.<ext>`. Depende del esquema `storage` de Supabase — no reproducible en Postgres local, mismo motivo que `0002_storage.sql`.
- `0023_fase3_bloqueA_perfil_administracion.sql` — **Fase 3, Bloque A: perfil unificado y administración.** `actor_profile_details` (bio/logo/portada, 1:1 con cualquier actor, auto-creada por trigger para actores nuevos y respaldada con backfill para los existentes); `actor_managers` (administración operativa delegable, distinta de `businesses.owner_id` — solo el propietario legal o un admin de plataforma agregan/revocan, nunca otro administrador); `actor_media` (galería preparada, interfaz de carga pendiente de Bloque C); `actor_search_index` (búsqueda básica con `tsvector` nativo de Postgres, sin motor externo, poblada por trigger desde nombre/bio/categoría — el Bloque B la extenderá con el catálogo). `public.actor_editable_by_current_user()` compone la pertenencia legal (Fase 2) con la administración operativa nueva — deliberadamente **no** se usa en ninguna política de `verifications`, que sigue exigiendo pertenencia legal exclusiva, sin cambios respecto a la Fase 2. Ver el análisis completo y los cuatro ajustes de producto aprobados en `PROJECT.md`.
- `0025_fase3_bloqueC_insignia_publica.sql` — **Fase 3, Bloque C, Entrega 1: insignia de verificación pública.** `public.actor_verification_badge(actor_id)` (`security definer`) calcula y devuelve únicamente el estado de verificación (`vigente`/`en_gracia`/`vencida`/`no_verificado`) para cualquier visitante, incluso anónimo — sin abrir ninguna política de lectura pública en `verifications` (que sigue siendo privada, Fase 2). Resuelve una bifurcación de permisos encontrada antes de implementar el perfil público: sin esta función, un visitante no podía calcular la insignia en absoluto. Ver el análisis de alternativas y la decisión aprobada en `PROJECT.md`.
- `0024_fase3_bloqueB_horarios_catalogo_ubicacion.sql` — **Fase 3, Bloque B: horarios, catálogo y ubicación estructurada.** `business_hours` (horario regular por día de la semana, múltiples intervalos, turnos que cruzan medianoche, 24 horas, días cerrados, con un `check` que exige exactamente una forma válida por fila y un trigger que impide intervalos superpuestos el mismo día — incluidos los que cruzan medianoche, vía descomposición en rangos de minutos); `business_special_hours` (excepciones por fecha, una fila por día, con prioridad absoluta sobre el horario regular); `business_open_status()` (función central en PostgreSQL, zona horaria `America/Guayaquil`, que calcula abierto ahora/próxima apertura/próximo cierre evaluando el horario de hoy y de ayer de forma **asimétrica a propósito** — un intervalo de hoy que cruza medianoche cubre desde su apertura hasta el fin del día, un intervalo de ayer solo se hereda si cruzaba medianoche y únicamente en su tramo previo al cierre); `business_catalog_collections`/`business_catalog_items` (colecciones flexibles por negocio, sin taxonomía rígida, con un trigger que impide asignar un ítem a una colección de otro negocio); extensión de `refresh_actor_search_index()` (Bloque A) para incluir el catálogo; `businesses.zone_id` (columna nueva, sin backfill — a diferencia de `places.area` en el Bloque 4, no existe ninguna fuente de datos confiable para poblarla, hallazgo documentado y no un defecto). Dos incidencias reales de cálculo de horario (contaminación del estado "abierto ahora" entre días, y "próximo cierre" no capturaba un turno nocturno en curso) se encontraron probando contra Postgres real y se corrigieron antes del commit — ver el detalle completo en `PROJECT.md`.
- `0026_fase3_bloqueC_ocultar_coleccion.sql` — **Fase 3, Bloque C, Entrega 4: ocultar colecciones del catálogo.** `business_catalog_collections.is_visible boolean not null default true`, con la política de lectura pública ajustada al mismo patrón ya usado por `business_catalog_items` (`(is_visible and business_visible_to_current_user(...)) or business_editable_by_current_user(...)`). Ver el hallazgo que la motivó y su aprobación en `PROJECT.md`.
- `0027_fase3_bloqueC_entrega6_seguimiento_unificado.sql` — **Fase 3, Bloque C, Entrega 6: consolidación del seguimiento bajo `interactions`.** Reconciliación puntual (una sola vez) de cualquier fila de `follows` (Fase 1, persona→persona) sin su equivalente en `interactions`, y eliminación de interacciones persona→persona sin respaldo real en `follows` — acotada estrictamente a ese par de tipos, sin tocar el seguimiento de negocios (ya vivía solo en `interactions` desde la Entrega 2). `public.reconcile_follows_to_interactions()` (solo admin) queda disponible para reconciliaciones futuras, pero **únicamente copia hacia adelante, nunca elimina** — se encontró y corrigió un defecto real durante la propia verificación: la versión inicial también borraba huérfanos, lo que habría destruido seguimientos nuevos legítimos si se reejecutaba después del corte. `public.prevent_self_interaction()` (trigger `before insert` en `interactions`) bloquea a nivel de base de datos seguirte a ti mismo, seguir tu propio negocio, o guardarlo como propietario/administrador operativo activo. `follows` no se elimina — queda como legacy de solo respaldo hasta un retiro controlado futuro. Ver el análisis completo y la incidencia encontrada en `PROJECT.md`.
- `0028_fase3_bloqueC_entrega6_documentar_reconciliacion_legacy.sql` — puramente documental (`comment on function`): deja constancia a nivel de base de datos de que `reconcile_follows_to_interactions()` es una herramienta legacy de una sola ejecución, no una operación normal del sistema — recomendación de arquitectura del Product Owner al aprobar la Entrega 6. No cambia ninguna columna, política ni comportamiento.
- `0029_fase3_bloqueC_entrega7_busqueda_actores.sql` — **Fase 3, Bloque C, Entrega 7: búsqueda y descubrimiento de negocios.** `public.search_actors(search_query, category_filter, actor_types)` sobre `actor_search_index` (Bloque A/B) — Actor-céntrica por diseño aprobado (`actor_types` es un arreglo, por defecto solo `'negocio'`, para no rediseñar la consulta cuando se agreguen otros tipos). El filtro `status = 'aprobado'` se aplica dentro de la propia función, nunca confiado solo a la política de lectura pública ya existente de `actor_search_index`. `category_filter` filtra por el id corto de categoría (misma taxonomía de `channels`), para los accesos rápidos por categoría de la pantalla de búsqueda. Sin ranking por popularidad/cercanía ni personalización — reservado para la Fase 6.
- `0030_fase4_bloque2_publicaciones.sql` — **Fase 4, Bloque 2: Publicaciones alimentan el Feed** (ver `FASE4_CONTRATO_ARQUITECTONICO.md`). Núcleo genérico `public.publications` (subtipo `'publicacion'` en este bloque, `'promocion'` se agregará en el Bloque 3 como una ampliación aditiva del `check`) + detalle `public.publication_posts` (cuerpo con límite de 500 caracteres exigido por restricción de base de datos, imagen única, categoría reutilizando la taxonomía de `channels`). `events` no se toca en absoluto. `public.actor_can_author_publication(check_actor_id)` — solo negocio/organizador con verificación `vigente` o `en_gracia` y edición del actor, o admin de plataforma para el Actor de sistema "Ahorita Editorial" (ningún actor de sistema tiene propietario legal ni administrador operativo). La verificación **solo** se exige al crear — nunca al editar/ocultar/eliminar, para que el contenido ya publicado siga siendo administrable si el negocio pierde la verificación después. `public.protect_publication_published_at()` (trigger) fija `published_at` la primera vez que el estado pasa a `'publicado'` y la protege de cualquier sobrescritura posterior — junto con `publication_posts.updated_at` (trigger propio), permite detectar "Editado" comparando ambas fechas sin falsos positivos, siempre que el detalle se cree antes de publicar (ver `lib/publications.js`). Verificado contra Postgres 16 real con roles de bajo privilegio: negocio verificado publica, negocio no verificado rechazado, propietario/administrador operativo activo con éxito, administrador revocado rechazado de inmediato (tanto al crear como al editar contenido ya existente), tercero ajeno rechazado, solo un admin de plataforma publica como "Ahorita Editorial", borrador/oculto invisibles fuera de su dueño, texto de más de 500 caracteres rechazado, y la transparencia de edición confirmada end-to-end. **Hallazgo corregido en el Bloque 3 (migración 0031, no reabre esta migración):** la política de `update` nunca volvía a comprobar la verificación después de la creación — ver la entrada de `0031` abajo.
- `0031_fase4_bloque3_promociones.sql` — **Fase 4, Bloque 3: Promociones alimentan el Feed** (ver `FASE4_CONTRATO_ARQUITECTONICO.md`). Amplía el `check` de `subtype` en `publications` para admitir `'promocion'` (la ampliación aditiva ya anticipada en el `0030`) y agrega el detalle `public.promotion_details` (título, beneficio con mínimo de 10 caracteres exigido por la base de datos, condición de canje, restricciones opcionales, vigencia `starts_at`/`ends_at` con `check (ends_at > starts_at)`, `ended_early_at`, precio opcional). `public.actor_can_author_promotion(check_actor_id)` — igual que `actor_can_author_publication`, pero el Actor de sistema "Ahorita Editorial" queda **permanentemente excluido**: un beneficio comercial no es contenido editorial, sin excepción. `public.actor_can_author_publication_of_subtype(check_actor_id, check_subtype)` enruta la autorización de creación según el subtipo, reemplazando la política de `insert` de `publications` para que sirva a ambos subtipos sin duplicar lógica. **Corrección del hallazgo de seguridad del Bloque 2:** `public.enforce_publication_publish_authorization()` (trigger `before update` en `publications`) bloquea la transición `borrador`/`oculto` → `publicado` cuando el actor ya no puede autorar contenido de ese subtipo — antes de este trigger, un negocio que perdía la verificación después de crear contenido podía publicar un borrador existente o reactivar contenido oculto, contradiciendo la regla de producto ya aprobada para pérdida de verificación (ver Bloque 2). Aplica por igual a `'publicacion'` y `'promocion'`; editar/ocultar/eliminar contenido ya existente sigue sin exigir verificación vigente. `public.promotion_status(p_publication_id, p_at)` es la única fuente de verdad computada sobre la vigencia (mismo patrón que `business_open_status()`), con seis fases: `borrador`, `oculto`, `programada_lejana`, `programada_proxima` (ventana de 24 horas antes de empezar), `vigente`, `finalizada_reciente` (ventana de gracia de 3 horas después de terminar) y `finalizada`. `public.list_feed_promotions()` es una función pública estrecha (mismo patrón que `search_actors()`) que solo expone las promociones publicadas cuya fase calculada es públicamente visible (`programada_proxima`, `vigente`, `finalizada_reciente`), evitando traer y descartar en el cliente lo que no corresponde mostrar. `public.protect_promotion_ended_early_at()` (trigger) hace que `ended_early_at`, una vez fijado, nunca pueda revertirse a `null`. `events` y el comportamiento existente de `publications`/`publication_posts` (Bloque 2) no se modificaron. Verificado contra Postgres 16 real con roles de bajo privilegio: negocio vigente crea y publica con éxito; negocio sin verificar rechazado incluso en borrador; "Ahorita Editorial" rechazado siempre; beneficio de menos de 10 caracteres y `ends_at <= starts_at` rechazados por la base de datos; un negocio que **pierde** la verificación (transición real `aprobado → vencido`, que por diseño del Bloque B de la Fase 2 solo puede ejecutar `service_role`) queda bloqueado al publicar una promoción en borrador o reactivar una publicación oculta — con el mensaje exacto "No tienes verificación vigente para publicar o reactivar este contenido" — pero conserva edición y eliminación sin verificación; las seis fases de `promotion_status()` verificadas mediante manipulación directa de `starts_at`/`ends_at`; inmutabilidad de `ended_early_at` confirmada; `list_feed_promotions()` verificado devolviendo únicamente fases públicas.

- `0032_fase5b_bloque1_fuente_de_verdad_interacciones.sql` — **Fase 5B, Bloque 1: cambio de fuente de verdad.** "Me gusta" y "guardado" de Eventos y Lugares migran de `post_likes`/`saved_events`/`saved_places` a `interactions`, cerrando la deuda dejada pendiente desde el Bloque 4 de la Fase 1. Reconciliación **bidireccional** (inserta lo que falta en `interactions`, elimina huérfanos que ya no tienen respaldo en la tabla legacy) — a diferencia del backfill puramente aditivo de la Fase 1, porque las tablas legacy siguieron recibiendo escrituras reales todo este tiempo. `public.sync_event_likes_count_from_interactions()` (`security definer`, corrige un defecto real encontrado durante la verificación: sin ese atributo, el contador no se actualizaba para un actor autenticado sin privilegios de administrador, por la misma política RLS que ya protegía a `events`) reemplaza al trigger viejo (`sync_event_likes_count`, deshabilitado sin eliminarse). `post_likes.target_type` deja de admitir filas nuevas de `'event'`/`'place'` (`check` `not valid`, sin afectar el historial) — sigue siendo la fuente activa de Comunidad (`status`/`question`), sin ningún cambio ahí. `saved_events`/`saved_places` quedan legacy de solo respaldo, mismo tratamiento que `follows`. Ver el análisis previo, el hallazgo de `security definer` y la verificación completa (incluida la reversión ejecutada de verdad) en `PROJECT.md`.

- `0033_fase5b_bloque2_reacciones_evento.sql` — **Fase 5B, Bloque 2: "Quiero ir" y "Ya fui".** Construye por primera vez las dos reacciones reservadas en `interactions.type` desde la Fase 1 (migración `0015`), nunca implementadas hasta ahora — exclusivas de Eventos, coexistentes sin exclusión mutua. Sin tabla ni columna nueva, sin cambio de RLS (la política pública de `interactions` desde la Fase 1, Bloque 4 ya las cubre). Una sola función, `public.enforce_event_reaction_timing()` (`security definer`, disparada `before insert on interactions`), resuelve tres reglas de negocio: (1) exclusividad de Eventos — rechaza `quiero_ir`/`ya_fui` contra cualquier otro `target_type`, reforzando en la base de datos lo que la interfaz ya no expone en Publicación/Promoción; (2) "ya_fui" nunca puede registrarse antes de `events.start_at`; (3) "quiero_ir" nunca puede registrarse por primera vez después de `coalesce(end_at, start_at)` — mismo criterio de "finalización" que ya usa `listUpcomingEvents` (`lib/events.js`) para decidir qué eventos siguen "próximos" en el feed. Ninguna de las tres reglas restringe `DELETE`: una intención histórica nunca se borra automáticamente y siempre puede retirarse, sin importar la fecha. Conteo público en vivo, sin columnas desnormalizadas ni triggers de conteo nuevos. Ver el análisis previo, el diseño aprobado y la verificación completa (incluida la reversión) en `PROJECT.md`.

- `0034_fase5b_bloque3_comentarios_generalizados.sql` — **Fase 5B, Bloque 3: comentarios generalizados sobre Evento y Publicación.** Generaliza los comentarios (antes exclusivos de Evento, sobre `event_comments`) al mismo eje `interactions`/`actors` que ya usan los demás tipos de interacción — Promoción queda deliberadamente excluida. Amplía el catálogo de `interactions.type` con `'comentario'` y **reemplaza la restricción única original** (`unique(actor_id, type, target_type, target_id)`) por un índice único parcial, `interactions_unique_toggle_idx`, que excluye `type = 'comentario'` de la unicidad — los seis tipos de alternancia conservan exactamente una fila por actor/objetivo, mientras que `comentario` admite filas ilimitadas del mismo actor sobre el mismo objetivo (necesario para permitir varios comentarios y para que la anonimización compartida de cuentas eliminadas nunca choque contra la unicidad). Siembra un nuevo Actor de sistema, "Cuenta eliminada" (mismo patrón que "Guía IA"/"Ahorita Editorial"), que puede agregar comentarios de varias cuentas eliminadas distintas sin que eso implique la misma autoría, y que está estructuralmente excluido de crear cualquier interacción nueva (sin `profile_id`, ninguna política de propiedad puede autorizarlo). `public.interaction_comments` es el detalle 1:1 de un comentario (`interaction_id` como llave primaria y foránea), con un `check` que exige exactamente dos estados — activo (`deleted_at is null`, `body` entre 1 y 500 caracteres tras `btrim`, protegido con `coalesce` contra la fuga de `NULL` de Postgres en un `CHECK`) o eliminado (`deleted_at is not null`, `body is null`) — y `parent_comment_id` autorreferenciado, preparado para respuestas anidadas pero sin exponerse todavía. `public.enforce_comment_rules()` (`security definer`, `before insert on interactions`) valida `target_type in ('event','publicacion')`, **rechaza explícitamente `subtype = 'promocion'`** (las Promociones viven en la misma tabla `publications`, así que el chequeo de `target_type` por sí solo no basta — un hallazgo real corregido durante la verificación, antes de que Promoción quedara comentable a nivel de base de datos), valida la visibilidad del contenido de destino, y aplica el límite de tasa (10 segundos entre comentarios, máximo 20 por hora, global por actor, contando también los ya eliminados — el soft-delete nunca evade el límite). `public.protect_comment_soft_delete()` (`before update on interaction_comments`) fuerza la única transición de `UPDATE` permitida (activo → eliminado) sin importar qué envíe el cliente, y rechaza tocar un comentario ya eliminado o su `parent_comment_id`; su política de RLS de `UPDATE` necesitó un `with check` explícito (otro hallazgo real: sin él, ni el propio dueño ni un admin podían completar la eliminación, porque RLS reutilizaba la misma condición `deleted_at is null` para validar la fila ya transicionada). Backfill procedural (bucle, no `insert...select` con `join`, para garantizar correspondencia 1:1 incluso con comentarios que compartan el mismo instante) migra `event_comments` campo por campo hacia `interactions`/`interaction_comments`, **deliberadamente antes** de crear el trigger de conteo `sync_comments_count()` (crearlo antes duplicaría el conteo sobre lo que el trigger legacy, todavía activo, ya dejó correcto — el mismo patrón de ordenamiento ya aprendido en el Bloque 1, y un defecto real que también se encontró y corrigió aquí). `sync_comments_count()` mantiene `events.comments_count` representando solo comentarios visibles; Publicación usa conteo en vivo. `event_comments_sync_count` queda deshabilitado (no eliminado) y la tabla `event_comments` permanece como respaldo legacy durante un periodo de convivencia. Verificado contra Postgres 16 real, recreando la base completa entre cada corrección: doble conteo, migración campo por campo, unicidad parcial contra los seis tipos y contra `comentario`, dos cuentas eliminadas reasignadas al mismo actor sobre Evento y Publicación sin conflicto, límite de tasa, soft-delete, RLS por rol (propio/ajeno/admin/Promoción/borrador/`target_type` inválido/invitado/actor de sistema), y reversión completa ejecutada de verdad en un escenario limpio (documentada honestamente como no garantizada después de uso real). Ver el análisis de 16 puntos, el addendum de unicidad, los cinco hallazgos corregidos y la verificación completa en `PROJECT.md`.

- `0035_fase6_bloque1_motor_afinidad.sql` — **Fase 6, Bloque 1: Motor de Afinidad** (ver `FASE6_FILOSOFIA_DESCUBRIMIENTO.md` y `FASE6_CONTRATO_ARQUITECTONICO.md`). Produce, para cada persona, una descripción legible y corregible de qué le interesa a partir de `interactions` — nunca decide qué se muestra en ningún Feed, responsabilidad fuera de alcance de este bloque. `public.affinity_contributions` es un registro **append-only** (ninguna fila ya escrita se actualiza ni se borra jamás, `enable row level security` sin ninguna política — ni siquiera su propia dueña puede leerla directamente) que acumula evidencia de cada interacción relevante; peso, confianza y estado se **calculan siempre en el momento de leer**, nunca se guardan precalculados — decisión tomada explícitamente para evitar la misma clase de defecto ya sufrida dos veces en este proyecto (el huérfano de `reconcile_follows_to_interactions` en la Fase 3, el doble conteo del Bloque 3 de la Fase 5B), ambos originados en un contador que debía mantenerse sincronizado con inserciones y eliminaciones de otra tabla. `public.record_affinity_contribution()` (`security definer`, `after insert on interactions`) resuelve categoría/zona según el tipo de contenido (Evento: solo categoría, sin zona estructurada; Lugar: categoría+zona; Publicación: categoría propia + zona heredada del negocio autor; actor seguido: dimensión propia; Promoción y cualquier otro: sin contribución, porque `promotion_details` nunca tuvo campo de categoría) y respeta la jerarquía de fuerza de señal ya establecida (`ya_fui` > `seguimiento` > `quiero_ir` > `guardado` > `comentario` > `me_gusta` > `compartir`) — nunca modifica `interactions`, que permanece exactamente tan pasivo como antes. `public.apply_affinity_correction(p_category, p_followed_actor_id, p_correction)` (`security definer`) implementa las tres acciones de corrección aprobadas ("atenuar"/"reiniciar"/"desconocido") **como filas agregadas, nunca como borrado ni mutación** — siempre resuelve el actor propio desde `auth.uid()`, nunca permite corregir el perfil de otra persona. `public.affinity_profile(check_actor_id)` (`security definer stable`) calcula el peso con decaimiento exponencial (media vida de 90 días) y un piso que impide llegar a cero (15% de la evidencia bruta acumulada), la confianza cualitativa, y el estado de corrección respetando una marca de agua con ventana de vigencia de 30 días (sin esa ventana, el estado habría quedado congelado como "recién corregido" para siempre, violando el principio de que el perfil es siempre la mejor interpretación disponible, nunca una verdad definitiva) — visible únicamente para su propia dueña, sin excepción para administradores. Categoría×zona se refina solo con concentración mínima (≥3 contribuciones con zona en la misma categoría), para no fabricar precisión de una sola coincidencia aislada. Alcance solo persona: un actor de tipo negocio no acumula afinidad propia. Tres defectos reales encontrados y corregidos durante la verificación contra Postgres 16 real, antes de cualquier commit: una constante referenciada antes de declararse, un tipo incompatible en `make_interval` (`days` exige `int`, no `numeric`), y una fuga de privacidad por comparación con `NULL` (mismo patrón ya visto en el Bloque 3 de la Fase 5B: `<>` contra `auth.uid() = null` evalúa `NULL`, no `true`, dejando pasar a un invitado anónimo — corregido con `is distinct from`). Verificado exhaustivamente: cómputo básico, privacidad/RLS completa, las tres correcciones, decaimiento con piso sobre 365 días simulados, garantía append-only (deshacer una interacción no borra su contribución histórica), umbral de refinamiento (2 vs. 3), y regresión de comentarios sobre Evento. Ver el análisis conceptual y técnico completo, los ajustes de producto aprobados y la verificación detallada en `PROJECT.md`.

- `0036_fase6_bloque1_resolucion_indirecta_y_revocacion.sql` — **Fase 6, Bloque 1, adenda: resolución indirecta por autor y revocación.** Corrige, hacia adelante (sin reabrir `0035`), dos brechas encontradas en auditoría antes de aprobar definitivamente el bloque. `public.resolve_affinity_category_zone(target_type, target_id)` centraliza la resolución de categoría/zona, compartida entre `record_affinity_contribution()` (reemplazado) y el nuevo `record_affinity_revocation()`: Promoción (nunca tuvo categoría propia) y Publicación sin categoría propia ahora heredan categoría/zona del negocio autor (siempre negocio/organizador con `business_id`, nunca una persona); Evento resuelve su zona vía `events.business_id → businesses.zone_id` (existente desde la migración `0010`, nunca usado hasta ahora); seguir/interactuar con un negocio/organizador ahora aporta también a su categoría/zona, además de la dimensión `actor_seguido` ya existente. Ninguna relación nueva infiere zona desde `lat`/`lng` (`zones` no tiene geometría). `public.record_affinity_revocation()` (`security definer`, `after delete on interactions`) inserta una fila de compensación append-only (`signal_type = 'revocacion'`, fuerza igual a la original en negativo) cuando se elimina una interacción alternable — nunca borra ni muta la contribución original; como el piso usa solo la fuerza positiva acumulada, la evidencia histórica se preserva mientras el peso decaído neto cae de inmediato hacia el piso y permanece ahí. No se dispara para `comentario` (su soft-delete nunca produce un `DELETE` real). **Defecto real encontrado durante el propio análisis, antes de escribir código:** sin una guarda explícita, este trigger habría violado la clave foránea `affinity_contributions.actor_id references actors(id) on delete cascade` durante la eliminación en cascada de una cuenta (el actor padre ya es invisible dentro de la misma transacción) — corregido comprobando primero que el actor propietario siga existiendo. Verificado contra Postgres 16 real (36 migraciones desde cero): las cinco filas de la matriz de resolución, revocación de los cuatro tipos alternables cayendo exactamente al piso calculado, soft-delete de comentario sin generar revocación, cascada de eliminación de cuenta ejecutada de verdad sin error de clave foránea, e imposibilidad estructural de reconstruir el objeto interactuado (`affinity_contributions` nunca tuvo columna `target_id`). Ver la matriz completa de resolución y el análisis de impacto en `PROJECT.md`.

- `0037_fase6_bloque1_estado_de_evidencia.sql` — **Fase 6, Bloque 1, segunda adenda: estado de evidencia.** Precisión final antes de la aprobación conceptual del bloque, sin rediseño. `affinity_profile()` gana `evidence_status` ('activa'/'historica'): 'activa' cuando el peso decaído sin piso (`decayed_weight`) sigue siendo positivo — hay al menos una fracción de evidencia todavía viva tras descontar cualquier compensación de revocación; 'historica' cuando ese valor ya cayó a cero o menos, es decir, cuando la dimensión solo sigue apareciendo por el piso mínimo, no por interés vigente. Criterio puramente aditivo sobre el cálculo ya existente — nunca rastrea qué contribución individual fue revocada (privacidad intacta), y distingue correctamente una revocación real (decae a cero/negativo) de la mera antigüedad de una señal nunca deshecha (el decaimiento natural de un valor positivo nunca cruza a cero en tiempo finito, así que permanece 'activa' aunque esté muy decaída). La confianza se degrada un nivel cuando `evidence_status = 'historica'` (alto→medio, medio→bajo). Re-auditó (sin cambios de código) la contribución multidimensional de seguimiento aprobada en `0036`: confirmado que produce exactamente una fila por dimensión válida por acción, sin duplicación. `AffinitySection.jsx` consume el nuevo campo con una etiqueta en lenguaje neutral, nunca una afirmación en presente sobre una acción ya deshecha. Verificado contra Postgres 16 real (37 migraciones desde cero): transición activa→histórica→activa en un ciclo seguir/dejar de seguir/volver a seguir con confianza degradada correctamente durante el estado histórico; revocación parcial (1 de 3 señales) permanece 'activa'; decaimiento puro de 365 días sin revocación permanece 'activa'; ledger de ambas dimensiones (categoria y actor_seguido) verificado con exactamente 3 filas cada uno tras el ciclo completo, confirmando ausencia de duplicación. Ver el detalle completo en `PROJECT.md`.

- `0038_fase6_bloque2_motor_garantias.sql` — **Fase 6, Bloque 2: Motor de Garantías** (ver `FASE6_CONTRATO_ARQUITECTONICO.md`, principio permanente incorporado: "la elegibilidad siempre ocurre antes que las garantías"). Produce, por carril (novedad, diversidad, equidad, serendipia), un conjunto de candidatos elegibles con su razón de explicabilidad — nunca decide el Feed final, nunca ordena, nunca fusiona con afinidad; el futuro Compositor (Bloque 3) decidirá composición, deduplicación e interleaving. Sin tabla ni trigger nuevo — seis funciones `security definer stable`, mismo patrón de "todo se calcula al leer" ya usado en `affinity_profile()`. `public.discoverable_content()` es el universo de contenido elegible (Eventos vigentes, Publicaciones y Promociones publicadas/vigentes, siempre de un actor negocio/organizador verificado `vigente`/`en_gracia`) — excluye por completo al actor de sistema "Ahorita Editorial", cuya inserción pertenece al Motor Editorial, un componente distinto. `public.geo_eligible(...)` es la restricción geográfica dura compartida (reutilizable por el futuro Compositor): zona elegida manualmente siempre gana sobre cualquier coordenada; sin geolocalización ni zona manual, degrada honestamente sin inferir ubicación de ninguna fuente (nunca por IP); un evento de planificación futura (más allá de una ventana cercana, constante de calibración) queda exento del radio estricto; contenido sin coordenada propia nunca se excluye por una distancia no calculable; usa la misma fórmula haversine ya empleada en `lib/directions.js`. `public.candidatos_novedad(...)` filtra por aparición reciente (7 días, constante). `public.candidatos_equidad(...)` exige baja frecuencia de publicación reciente del actor (≤1 en 30 días, constante) **sobre los mismos filtros base de vigencia y verificación** — la equidad nunca los releva, solo agrega un criterio más. `public.candidatos_diversidad(...)` etiqueta todo el contenido elegible con un tope de 3 candidatos por actor (constante), para evitar que uno solo domine el conjunto entregado, sin decidir posiciones consecutivas ni cuotas finales — eso pertenece al Compositor. `public.candidatos_serendipia(check_actor_id, ...)` ordena mediante una rotación **determinística** (`hashtext(actor || día_calendario || target)`), nunca `random()` (incompatible con `stable` y con reproducibilidad) ni memoria de exposición persistida — mismo actor y mismo día producen siempre el mismo orden; el día siguiente renueva la rotación automáticamente; excluye únicamente las categorías donde `affinity_profile()` ya muestra `confidence = 'alto'` y `evidence_status = 'activa'` (umbral conservador). Verificado contra Postgres 16 real (38 migraciones desde cero): elegibilidad de base (negocio sin verificar excluido, evento admin sin negocio incluido sin exigir verificación), ventanas de novedad/equidad, tope de diversidad por actor, estabilidad/renovación/diferenciación por actor de la rotación de serendipia (confirmado con el mismo cálculo de hash sobre fechas distintas), las ocho combinaciones de `geo_eligible` (autorizada/sin permiso/zona manual en ambos sentidos/sin coordenada/planificación futura/evento cercano en el tiempo), carril vacío sin relleno artificial, candidato calificando para dos carriles sin deduplicación prematura, y regresión completa del Bloque 1 sin cambios. Radio de cercanía (5 km) y ventana de exención por planificación futura (3 días) quedan señalados explícitamente para revisión — constantes adicionales necesarias para operar `geo_eligible()`, distintas de las cuatro ya fijadas en la adenda técnica. Ver el análisis conceptual, la adenda técnica y la verificación completa en `PROJECT.md`.

- `0039_fase6_bloque2_calibracion_geografica.sql` — **Fase 6, Bloque 2, adenda: calibración geográfica.** Corrige, hacia adelante (sin reabrir `0038`), las dos constantes geográficas señaladas explícitamente en el informe final del bloque. `public.discovery_calibration()` centraliza **todas** las constantes de este bloque (no solo las geográficas) como única fuente de verdad — ninguna función vuelve a declarar estos valores como literales propios. `public.geo_eligible(...)` reemplaza el radio único de 5 km por dos radios según la **temporalidad** del contenido, nunca su categoría: radio inmediato (3 km, Promociones y Publicaciones siempre, Eventos dentro de la ventana cercana) y radio de planificación (15 km, exclusivamente Eventos cuyo inicio supera el umbral de 3 días — nunca Promociones ni Publicaciones, estructuralmente, porque `p_event_start_at` es `NULL` para esos subtipos). La exención total de radio para planificación futura queda eliminada — el radio ampliado nunca anula la restricción por completo. La función también deja de devolver un booleano: ahora devuelve un estado (`zona_manual`/`confirmada_cercana`/`planificacion_futura`/`sin_restriccion`/`NULL` para no elegible), expuesto como nueva columna `geo_status` en las cuatro funciones `candidatos_*`, para que el futuro Compositor pueda distinguir cercanía confirmada de ausencia de restricción evaluable y nunca muestre "está cerca de ti" sobre contenido cuya proximidad nunca se verificó. Verificado contra Postgres 16 real (39 migraciones desde cero): las diez combinaciones geográficas pedidas explícitamente (promoción/evento dentro y fuera de cada radio, evento dentro del umbral fuera del radio inmediato correctamente excluido, evento más allá del umbral dentro y fuera del radio de planificación, zona manual en ambos sentidos, contenido con zona sin coordenadas, contenido sin ninguna ubicación, sin permiso de ubicación, ausencia estructural de cualquier parámetro de IP), verificación end-to-end con coordenadas reales a través de `candidatos_novedad()`, y regresión completa del Bloque 1 y de los cuatro carriles sin cambios de comportamiento. Ver el detalle completo en `PROJECT.md`.

- `0040_fase6_bloque3_motor_editorial.sql` — **Fase 6, Bloque 3: Motor Editorial** (ver `FASE6_CONTRATO_ARQUITECTONICO.md`, principio permanente incorporado: "Editorial nunca existe para corregir al algoritmo; existe para aportar criterio humano allí donde el algoritmo, por naturaleza, nunca puede sustituirlo"). Decide qué contenido curado por el equipo está seleccionado y produce el universo completo de candidatos editoriales elegibles — nunca decide cantidad, posición ni interleaving, responsabilidad exclusiva del futuro Compositor. `public.editorial_selections` es **una fila única y mutable por `(target_type, target_id)`** (restricción `unique`), no un ledger append-only — a diferencia de `affinity_contributions` (Bloque 1) o `verifications` (Fase 2), aquí no existe ningún proceso automático ni concurrente que dependa de una cronología completa: es una decisión humana, manual, de un único rol (`is_admin()`). Reactivar una selección previa reutiliza la misma fila (`created_at` protegido por trigger, inmutable incluso ante escritura directa); no conserva el historial completo de cada ciclo de selección/revocación anterior — una simplificación deliberada, documentada como tal. **Sin ninguna política de `INSERT`/`UPDATE`/`DELETE`** en la tabla cruda — ni siquiera `is_admin()` puede escribirla directamente; el único camino de escritura sancionado son `public.set_editorial_selection(...)` (crea, reactiva o modifica) y `public.revoke_editorial_selection(...)` (retira, nunca elimina), ambas `security definer`, que garantizan por construcción que `decided_at`/`decided_by` representen siempre la decisión vigente y que `revoked_at`/`revoked_by` se limpien o se fijen siempre juntos (reforzado además por un `check` de pareo a nivel de tabla, verificado incluso contra un intento de escritura directa que la sortea). Un trigger de validación (`validate_editorial_selection_target()`) exige que el contenido referenciado exista de verdad y rechaza explícitamente una Publicación con `subtype = 'promocion'` aunque `target_type = 'publicacion'` — exclusión permanente de Promociones. `public.candidatos_editorial(...)` devuelve **todos** los candidatos elegibles, sin límite de cantidad (cantidad/posición quedan reservadas por completo al Compositor) y expone `reason_code` crudo (enum técnico cerrado) en vez de una frase ya construida — la redacción visible es una capa de presentación separada, fuera de este bloque. Su universo de elegibilidad de base es deliberadamente **opuesto** al de `discoverable_content()` (Bloque 2): permite al actor de sistema "Ahorita Editorial" y a eventos sin actor de negocio/organizador detrás, exigiendo verificación `vigente`/`en_gracia` únicamente cuando el autor sí es negocio/organizador — la bandera editorial nunca hace elegible a un autor no autorizado, contenido oculto o vencido, porque siempre se cruza contra los mismos filtros base. Reutiliza `geo_eligible()` del Bloque 2 sin duplicar ninguna constante — no se creó ninguna función de calibración propia, porque al quitar el límite de cantidad no quedó ninguna constante nueva que calibrar; `discovery_calibration()` sigue siendo la única fuente para todo el sistema de descubrimiento. `public.editorial_selection_public(...)` es la lectura pública segura (mismo patrón que `actor_verification_badge()`): expone `reason_code`/vigencia/estado activo-revocado, pero **nunca** `decided_by` ni `revoked_by` — la identidad del administrador queda reservada a auditoría (`SELECT` directo sobre la tabla cruda, solo `is_admin()`). Backfill de `events.editor_pick` con administrador determinístico (el más antiguo registrado); si no existe ningún administrador, la migración falla explícitamente en vez de inventar una identidad. `events.editor_pick` queda legacy, sin nuevas escrituras desde la aplicación — `pickEditorSelection()` (`src/lib/feed.js`) y el panel de administración de eventos ya leen/escriben exclusivamente contra el nuevo mecanismo (`src/lib/editorial.js`). Verificado contra Postgres 16 real (40 migraciones desde cero): backfill exacto de los 3 eventos ya marcados sin duplicados; fallo explícito del backfill simulado sin administrador; los 7 candidatos elegibles esperados (3 migrados + 2 eventos + 2 publicaciones) frente a autor con verificación revocada, contenido oculto, evento finalizado y Promoción — todos ausentes tal como se esperaba; Promoción rechazada al intentar seleccionarla aunque `target_type = 'publicacion'`; contenido inexistente rechazado; ventana `starts_at`/`ends_at` inválida rechazada por el `check`; negocio intentando autoseleccionarse rechazado (no es admin); reactivación completa (revocar → reseleccionar con un segundo admin) con `created_at` preservado y `decided_at`/`decided_by` actualizados; `geo_status` correcto con coordenadas reales (incluida planificación futura) y con zona manual; cero candidatos sin relleno tras revocar todo; selección vencida (`ends_at` pasado) ausente y la misma publicación con ventana vigente presente; RLS confirmada con roles de bajo privilegio (lectura cruda denegada a no-admin, permitida a admin, escritura directa denegada incluso con `GRANT` y siendo admin porque no existe ninguna política que la permita); reversión completa ejecutada de verdad en un escenario limpio (`editorial_selections` eliminada sin afectar `events`/`publications`/`editor_pick`, documentando honestamente que las selecciones nuevas de Publicaciones se perderían al revertir); regresión completa del Bloque 1 y el Bloque 2 sin cambios de comportamiento. Build y lint limpios. Verificación visual en navegador (Playwright) **no pudo ejecutarse** en este entorno por no contar con una instancia local de Supabase corriendo — ver la nota honesta en `PROJECT.md`. Ver el análisis conceptual, el diseño técnico (con sus cuatro ajustes arquitectónicos) y la verificación completa en `PROJECT.md`.

  **Corrección preproducción (antes del primer despliegue real, ver `PROJECT.md`):** el backfill de `events.editor_pick` descrito arriba —con su fallo explícito si no existía administrador— se extrajo de esta migración hacia `supabase/scripts/backfill_editorial_selections.sql`, ejecutado una vez, manualmente, después de `bootstrap_admin.sql`. Motivo: esa dependencia de un dato operativo dentro de una migración de esquema impedía aplicar las 46 migraciones deterministas contra una base de datos vacía sin intervención manual previa — bloqueo que se habría repetido en cualquier restauración de respaldo, entorno de CI o proyecto nuevo futuro. `0040`, tal como queda en el repositorio, ya no incluye el backfill — solo el DDL del Motor Editorial descrito arriba. Es la única de las 46 migraciones corregida reabriendo su propio archivo en vez de hacia adelante, justificado porque ninguna versión había llegado a aplicarse contra ningún proyecto remoto y porque este defecto aborta la aplicación completa antes de que cualquier migración posterior pueda alcanzarse. Verificado contra `ahorita-production` real: las 46 migraciones se aplicaron sin error en una sola pasada; el backfill posterior migró los mismos 3 eventos, sin duplicados, de forma idempotente ante una segunda ejecución (confirmado con `candidatos_editorial()`, que solo mostró 2 de los 3 por vigencia propia del evento — `coalesce(e.end_at, e.start_at) >= now()` en `editorial_eligible` —, no por ningún defecto del backfill).

- `0041_fase6_bloque4_compositor_feed.sql` — **Fase 6, Bloque 4: Compositor del Feed** (ver `FASE6_CONTRATO_ARQUITECTONICO.md`, cuatro principios permanentes incorporados: el Compositor nunca intenta ser inteligente; el anti-monopolio es transversal a las seis entradas; Editorial reclama por completo cupo/propiedad/explicación; las proporciones son siempre relativas, nunca una cantidad fija de posiciones). Último componente de la Fase 6 — fusiona las seis entradas ya elegibles (Afinidad, Novedad, Diversidad, Equidad, Serendipia, Editorial) en una composición determinista, deduplicada, con anti-monopolio y paginación por clave de identidad. `discovery_calibration()` se extiende (no una función nueva) con las seis proporciones del Compositor (afinidad 35%/novedad 15%/diversidad 20%/equidad 10%/serendipia 10%/editorial 10% — calibración inicial, ajustable por migración futura) y las constantes de anti-monopolio/estabilidad (ventana de 10, topes por actor/categoría/zona/tipo, brecha editorial mínima, bloque de estabilidad de 4 horas — nunca un día completo). Verificado que ningún objeto depende físicamente de `discovery_calibration()` a nivel de `pg_depend` — basta con recrearla, sin tocar sus consumidores. `candidatos_editorial()` (Bloque 3) se extiende con `zone_id` (ya tenía `category`/`actor_id`) sin tocar ninguna regla de elegibilidad. `public.candidatos_afinidad(check_actor_id, ...)` (nueva): la pieza que le faltaba a Afinidad — lee `discoverable_content()` (Bloque 2) cruzado contra `affinity_profile()` por categoría o actor seguido (confianza `medio`/`alto`, `evidence_status='activa'`); sin `check_actor_id`, cero candidatos, nunca personalización fabricada; aplica ya aquí el mismo tope de candidatos por actor que Diversidad, para que un único actor no sature por sí solo el cupo de Afinidad. `public.compose_feed(...)` (el Compositor): deduplica por `(target_type, target_id)` — Editorial reclama siempre por completo un ítem compartido; entre las cinco entradas restantes, la propiedad la decide primero la escasez (menos candidatos totales gana) y, en empate, una rotación determinística por hash — nunca una lista de prioridad fija que pueda dejar sistemáticamente en cero a un carril con candidatos reales. Ordena mediante colas justas ponderadas (`clave = rango_dentro_del_carril / peso_del_carril`, sin ninguna fórmula de puntuación), aplica anti-monopolio (ventana deslizante + no-consecutivos-del-mismo-actor + brecha editorial mínima) en una pasada lineal que difiere violaciones a una segunda pasada (nunca descarta contenido elegible), y pagina por `(target_type, target_id)` — nunca `OFFSET`. **Dos hallazgos reales corregidos durante la verificación**: (1) la propiedad por prioridad fija dejaba a Diversidad/Serendipia en cero pese a candidatos reales cuando su universo coincidía con el de Novedad — corregido hacia escasez+hash, verificado con concentración de actor variable; (2) la segunda pasada de anti-monopolio colocaba diferidos en su orden fijo original, repitiendo violaciones entre sí — corregido a una búsqueda del primer diferido no violatorio en cada paso. **Rendimiento medido con `EXPLAIN ANALYZE`** contra 210 eventos/69 negocios: la comprobación de ventana con `unnest()+count()` correlacionado costaba 663 ms/página; reemplazada por `array_positions()+cardinality()` nativo, baja a 115 ms/página (~5.8×) sin cambiar ningún resultado. Verificado contra Postgres 16 real (41 migraciones desde cero): deduplicación sin duplicados hasta con 300 ítems; filtro de canal; paginación sin solapamiento; RLS con `authenticated` (negocio no-admin) y `anon`; `affinity_profile()` de un actor `negocio` devuelve cero filas (sin concepto de afinidad propia, ya establecido desde el Bloque 1); **reversión ejecutada de verdad con un hallazgo real** — `discovery_calibration()` y `candidatos_editorial()` fueron extendidas en el mismo lugar, no creadas de nuevo, así que revertir el Bloque 4 exige restaurarlas a su forma exacta de Bloque 2/3, no solo eliminarlas (confirmado que sin esa restauración, `candidatos_novedad()` y el resto del Bloque 2 quedan rotos); con la restauración correcta, todo vuelve exactamente a como estaba, sin pérdida de datos. Regresión completa de los Bloques 1-3. `src/lib/feed.js` gana `getComposedFeed()` (reutiliza sin cambios las funciones de obtención y mapeo ya usadas por `getFeed()`, que se conserva intacta como camino de reversión); `FeedPage.jsx` la adopta con paginación mínima ("Cargar más") y la razón de composición como subtítulo. Build y lint limpios. Playwright no ejecutado por la misma limitación de entorno ya declarada en el Bloque 3. Ver el diseño técnico (25 puntos + 7 de revisión), los hallazgos y la verificación completa en `PROJECT.md`.

## Convertir tu cuenta en administradora

Después de registrarte en la app, corre esto en el **SQL Editor** de Supabase (reemplaza el correo):

```sql
update public.profiles set is_admin = true
where id = (select id from auth.users where email = 'tu-correo@ejemplo.com');
```

Con eso podrás entrar a `/admin` para aprobar negocios, verificar respuestas y cargar lugares sin tocar código.

## Sembrar el primer administrador y el backfill del Motor Editorial (proyecto nuevo, desde cero)

Para un proyecto recién creado, después de `supabase db push --include-all`, el orden correcto es:

1. Crear la cuenta Auth real del administrador (dashboard o la app) y confirmar que el disparador de creación de `profiles` generó su fila.
2. Ejecutar `supabase/scripts/bootstrap_admin.sql` — herramienta operativa parametrizada, deliberadamente fuera de `supabase/migrations/`:
   ```
   psql "$CONNECTION_STRING" -v admin_id="$ADMIN_ID" -f supabase/scripts/bootstrap_admin.sql
   ```
   Falla explícitamente si el `id` no corresponde a ningún perfil ya existente, o si ya existe un administrador distinto — nunca crea un segundo administrador silenciosamente ni inventa una identidad.
3. Ejecutar `supabase/scripts/backfill_editorial_selections.sql` — mismo patrón, idempotente, ejecutado una sola vez:
   ```
   psql "$CONNECTION_STRING" -f supabase/scripts/backfill_editorial_selections.sql
   ```
   Migra los eventos legacy con `editor_pick = true` hacia `editorial_selections`, atribuidos al administrador recién sembrado. Ver la nota de corrección preproducción de `0040` (arriba) para el porqué de que este paso ya no sea automático dentro de las migraciones.
