# AHORITA — BETA READINESS CHECKLIST

**Subordinada a `ETAPA_PRODUCTO_VIVO.md`.** No es un documento canónico, no tiene autoridad conceptual propia, no introduce ningún contrato arquitectónico ni ninguna filosofía nueva. Es una **checklist operativa viva**: una lista de verificación con criterio objetivo por ítem, pensada para marcarse conforme avanza la preparación real de Ahorita para recibir personas y negocios reales en Cuenca.

**Creada el 2026-07-25**, tras el cierre de la Fase 7 y la adopción de `ETAPA_PRODUCTO_VIVO.md`. No ocupa ningún número de fase de `MASTERPLAN.md` — ese documento ya la referencia en su sección "Etapa — El Producto Vivo (entre la Fase 7 y la Fase 8)" a través de la autoridad de `ETAPA_PRODUCTO_VIVO.md`, sin necesidad de mencionar este archivo directamente.

**Cómo usarla.** Cada ítem tiene un objetivo, un criterio objetivo de verificación, la evidencia que debe conservarse, y un estado. El estado inicial de todo ítem es **Pendiente**; se actualiza a **En progreso** o **Hecho** solo cuando la evidencia exigida exista realmente — nunca por intención o por trabajo parcial. Ningún ítem se marca "Hecho" sin su evidencia correspondiente conservada (capturas, registros, logs, documentos reales).

**Qué no es.** No reemplaza ni compite con ningún documento canónico. No introduce funcionalidad nueva — cada ítem es, o una verificación de algo ya construido, o una configuración operativa mínima ya implícita en decisiones previas del proyecto. Si en el camino de completarla surge una decisión conceptual nueva (por ejemplo, qué hacer si el costo real de la Guía IA excede el presupuesto aceptado), esa decisión se documenta en `PROJECT.md` como lo que es — una decisión puntual —, no se disuelve dentro de esta checklist ni la convierte en un documento distinto.

---

## A. Condiciones bloqueantes antes de admitir a la primera persona real

| # | Objetivo | Criterio objetivo | Evidencia a conservar | Estado |
|---|---|---|---|---|
| A1 | Infraestructura Supabase de producción operativa | Las 46 migraciones aplicadas sin error, en orden, contra el proyecto real; cada tabla nueva responde a una consulta real; adicionalmente, para el Motor Editorial (migración `0040`, corregida antes de este despliegue — ver nota bajo esta tabla): las 4 funciones públicas confirmadas por catálogo del sistema, y las 3 selecciones editoriales legacy verificadas (conteo exacto, `target_type`, `decided_by`, sin duplicados, idempotencia de `backfill_editorial_selections.sql` confirmada por segunda ejecución) | `db_push_output.txt` + `migration_list_post_push.txt` + resultados conservados de las 7 consultas de verificación del Motor Editorial | Hecho |
| A2 | Frontend desplegado, PWA validada | Instalable sobre HTTPS; actualización a versión nueva sin romper sesión; invalidación de caché correcta; recuperación tras pérdida de conexión; cierre de sesión sin datos privados residuales visibles; probado en un Android real y un navegador de escritorio | Recorrido completo verificado en escritorio (Edge/DevTools, inspección forense de Cache/Local/Session Storage/IndexedDB) y en un Android real (instalación, actualización con sesión preservada, pérdida/recuperación real de conexión, logout persistente); ver el detalle completo y las dos limitaciones metodológicas explícitas en `PROJECT.md` ("A2 — CERRADO") | Hecho |
| A3 | Secretos y credenciales de producción propios y separados de desarrollo | Todas las credenciales y secretos utilizados por los componentes actualmente desplegados en producción son exclusivamente de producción y están separados de los utilizados en desarrollo o pruebas. Se verifica tanto la configuración del entorno como el artefacto real desplegado desde un dispositivo fuera de la red de desarrollo. A3 no exige desplegar anticipadamente servicios o Edge Functions cuya puesta en producción y validación funcional corresponda a otros ítems de esta checklist. | Grep exhaustivo del repositorio sin secretos/tokens hardcodeados; inspección real de Network en producción confirmando conexión al project ref de `ahorita-production`; verificación cruzada en el dashboard de Supabase del mismo project ref y de la clasificación oficial anon/public vs service_role/secret; Vercel Production con únicamente las dos variables esperadas (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`); ver el detalle completo y la limitación metodológica explícita en `PROJECT.md` ("A3 — CERRADO") | Hecho |
| A4 | Buckets de Storage reales con políticas RLS | Subir/leer un archivo real; un archivo privado de evidencia de verificación permanece inaccesible públicamente | Bucket `media`: subida autenticada y lectura pública confirmadas; subida no autenticada rechazada por RLS (`403`/`AccessDenied`/"row-level security policy" en el cuerpo). Bucket `verification_evidence`: objeto real confirmado existente en el Dashboard, no recuperable mediante una petición sin sesión contra el endpoint privado (`404`/`NoSuchKey`/"Object not found" — comportamiento documentado de Supabase Storage al enmascarar denegaciones de RLS como "no encontrado" en buckets privados); ver el detalle completo y las fuentes en `PROJECT.md` ("A4 — CERRADO") | Hecho |
| A5 | Registro/login real de punta a punta, incluyendo cambio de dispositivo | Una persona real se registra, confirma correo, cierra e inicia sesión sin intervención del equipo; además, esa misma persona inicia sesión en un segundo dispositivo y conserva sus guardados, seguimientos y preferencias sin pérdida | Registro self-service real (correo real, confirmación real, login/logout/login sin intervención del equipo) verificado contra `ahorita-production`; cambio de dispositivo (PC↔Android real) verificado para guardado de lugar, seguimiento y preferencia de interés, cada uno confirmado en el segundo dispositivo sin repetir la acción; ver el detalle completo en `PROJECT.md` ("A5 — CERRADO") | Hecho |
| A6 | Matriz de permisos RLS reproducible contra JWT reales | Tabla con columnas rol / recurso / operación / caso permitido / caso prohibido / resultado esperado / resultado real, cubriendo persona común, negocio/organizador y administrador, incluyendo intentos cruzados entre dos personas distintas — sin ninguna fila en rojo | La matriz completa, llenada con resultados reales | Pendiente |
| A7 | Trigger de creación de `profiles` contra Auth real | Un registro real produce exactamente una fila de perfil, sin duplicados ni huérfanos | Consulta de solo lectura contra `ahorita-production` (`auth.users` vs `public.profiles`): 4 usuarios reales, 4 perfiles, cero huérfanos en cualquier dirección, cero duplicados, verificación puntual positiva para dos cuentas reales; ver el detalle completo en `PROJECT.md` ("A7 — CERRADO") | Hecho |
| A8 | Guía IA: control de gasto antes del primer uso real | Estimación conservadora + alerta/tope real configurado en el proveedor + presupuesto máximo aceptado explícito + mecanismo que puede cortar temporalmente solo el acceso a la Guía IA sin afectar el resto de la app | Captura de configuración del proveedor + prueba del mecanismo de corte | Pendiente |
| A9 | Límite de tasa por persona verificado en producción | Una persona real enviando mensajes muy rápido recibe una respuesta de límite clara, no un error genérico | Prueba real documentada | Pendiente |
| A10 | `export-user-data` contra Auth Admin API real | Exportación real de una cuenta de prueba contiene exactamente sus datos, sin fuga de otra persona | Exportación real de Pedro (`export-user-data`, VERSION 4 en `ahorita-production`) contiene su `profile`, `consent_records` y su interacción real (`interactions`: `guardado`/`place`/`Parque Calderón`), sin ningún dato de otra cuenta; defecto real encontrado en la primera exportación (`interactions` ausente por desactualización respecto a la migración `0032`), corregido en el commit `f0143b4` y confirmado con una nueva exportación real; ver el detalle completo en `PROJECT.md` ("A10 — CERRADO") | Hecho |
| A11 | `process-account-deletions` contra Auth Admin API real | Solicitud real: crear, cancelar, ejecutar; reasignación real a "Cuenta eliminada"; incluye el caso de borrado parcialmente ya ejecutado (H2) | Registro del ciclo completo ejecutado | Pendiente |
| A12 | Invocación periódica confirmada para procesos programados | `pg_cron`, programador externo, o decisión explícita documentada de invocación manual del fundador a esta escala | Configuración o decisión documentada | Pendiente |
| A13 | Términos de servicio y política de privacidad mínimos | Documento real, accesible desde la app, publicado antes del primer registro real | Documento real publicado en `/legal` (ruta pública, verificada sin sesión en producción); enlace visible desde "Ajustes" y desde el flujo de registro antes de crear cuenta. **DESVIACIÓN HISTÓRICA:** no se publicó antes de los primeros registros reales — existieron registros reales (Moisés, Pedro, Ana y las cuentas de prueba de A5/A6) antes del 15 de agosto de 2026, fecha real de publicación; ver el detalle completo en `PROJECT.md` ("A13 — CERRADO, con desviación histórica") | Hecho, con desviación histórica documentada |
| A14 | Respaldo restaurado en entorno aislado (nunca sobre la base activa) | Backup restaurado con éxito; tablas y relaciones íntegras; estrategia de Auth+Storage compatible verificada; estimación real de punto y tiempo de recuperación (RPO/RTO) | Registro de la restauración de prueba + cifras de RPO/RTO | Pendiente |
| A15 | Alerta mínima ante fallo crítico | Notificación real al fundador dentro de minutos de un fallo simulado (Guía IA caída, feed no carga, función fallando repetidamente) | Prueba de fallo simulado y notificación recibida | Pendiente |
| A16 | Correo transaccional de producción | Confirmación de registro y recuperación de contraseña funcionan con remitente correcto, URLs de redirección de producción, SPF/DKIM si aplica, probado con más de un proveedor de correo, sin enlaces a localhost ni secretos expuestos | Correo real de recuperación (Gmail) y correo real de confirmación de una cuenta nueva self-service (Yahoo Mail), ambos con `redirect_to=https://ahorita-five.vercel.app`, remitente `Supabase Auth <noreply@mail.app.supabase.io>` con SPF/DKIM/DMARC en PASS; hallazgo histórico de un correo con `redirect_to=http://localhost:3000` (7 de agosto) documentado como defecto ya corregido, no oculto; ver el detalle completo en `PROJECT.md` ("A16 — CERRADO") | Hecho |
| A16-bis | Notificación push transaccional de producción | Una notificación push real del sistema (ej. "tu negocio fue verificado") se entrega correctamente usando las claves VAPID de producción, no las de desarrollo, con la app instalada como PWA real | Captura de la notificación recibida + confirmación de que las claves usadas son las de producción | Pendiente |
| A17 | Plan de reversión del despliegue | Versión estable identificada; frontend y Edge Functions revertibles; procedimiento ensayado al menos una vez; nunca revierte destructivamente datos ya creados por personas reales; política clara de migraciones de avance/reparación | Registro del ensayo de rollback | Pendiente |
| A18 | Instrumentación mínima de aprendizaje | Captura agregada de registros, usuarios activos, retención básica, errores, uso de Feed/mapa, conversaciones con la Guía IA, tasa de `noAnswer`, costo de IA — nunca contenido literal de conversación, nunca alimentando Afinidad o Conocimiento Permanente | Panel o consulta funcionando con datos de prueba | Pendiente |
| A19 | Canal de soporte visible y probado | Un mensaje real de reporte de fallo, solicitud de ayuda o ejercicio de derecho de privacidad llega a una persona responsable | Correo real de prueba enviado al canal de soporte (visible en "Ajustes" desde el commit `05041a6`), recibido por la persona responsable, con respuesta real registrada (~1 minuto de tiempo de respuesta); ver el detalle completo en `PROJECT.md` ("A19 — CERRADO") | Hecho |
| A20 | Inventario de datos operativos/semillas verificado | Administrador inicial, "Ahorita Editorial", "Cuenta eliminada", ciudades/zonas necesarias y cualquier configuración sin la cual un flujo crítico falle — todos existen, documentados, con dependencias verificadas | Consulta real de solo lectura contra `ahorita-production`, verificación uno a uno: administrador inicial (1), actor de sistema "Ahorita Editorial" (1), actor de sistema "Cuenta eliminada" (1), ciudad "Cuenca" (1), zonas "Centro Histórico" y "Turi" (1 cada una), catálogo de 10 canales completo, sin faltantes; ver el detalle completo en `PROJECT.md` ("A20 — CERRADO") | Hecho |
| A21 | Ensayo end-to-end con un negocio real reclutado para este fin | Ciclo completo de verificación (Fase 2) ejecutado sin atajos manuales en la base de datos: solicitud → evidencia real → aprobación → visible como verificado | Registro del ciclo completo | Pendiente |
| A22 | Ensayo end-to-end con una persona real reclutada para este fin | Recorrido completo: registro → confirmación → exploración → Feed/mapa → Guía IA → interacción social o guardado → privacidad → cierre de sesión → regreso posterior, sin que el equipo explique dónde tocar | Registro/video del recorrido | Pendiente |

## Nota — corrección preproducción de la migración `0040` (Motor Editorial)

Antes del primer `db push` real contra `ahorita-production`, se encontró que `0040_fase6_bloque3_motor_editorial.sql` incluía, en la misma transacción que su DDL, un backfill de `events.editor_pick` que exigía un administrador ya existente y abortaba toda la secuencia de migraciones si no lo encontraba — impidiendo que las 46 migraciones se aplicaran deterministas contra una base de datos vacía, sin intervención manual previa. Se corrigió extrayendo ese backfill a `supabase/scripts/backfill_editorial_selections.sql` (idempotente, ejecutado una vez, manualmente, después de `bootstrap_admin.sql`), dejando `0040` como DDL puro. Es la única de las 46 migraciones corregida reabriendo su propio archivo en vez de hacia adelante — justificado porque ninguna versión había llegado a aplicarse contra ningún proyecto remoto y porque este defecto aborta la aplicación completa antes de que cualquier migración posterior pueda alcanzarse. Ver el análisis completo, la prueba de equivalencia funcional y la verificación contra `ahorita-production` real en `PROJECT.md`.

Esta corrección deja una condición permanente para cualquier aplicación futura desde cero (restauración de respaldo — A14 —, CI, o un entorno nuevo): **el backfill del Motor Editorial ya no es automático** y debe ejecutarse manualmente, en este orden, después de `db push --include-all`: (1) crear la cuenta Auth real del administrador, (2) `bootstrap_admin.sql`, (3) `backfill_editorial_selections.sql`. A14, al probarse, debe verificar explícitamente que este paso no se omitió.

## Nota — alcance de A3 aclarado antes de su verificación

Un diagnóstico previo a cualquier implementación (2026-08-09) encontró que el criterio original de A3 combinaba un alcance amplio con una evidencia angosta, lo que lo habría hecho depender de que otros ítems de esta misma tabla (A8, A9, A10, A11, A16-bis) ya estuvieran resueltos. Se aclara el alcance de A3 hacia la higiene y separación de secretos de lo que ya está desplegado en producción hoy — sin exigir el despliegue anticipado de ningún servicio o Edge Function, cuya puesta en producción y validación funcional pertenece a sus propios ítems.

## Nota — aclaración de alcance de A5 ("historial")

Un diagnóstico previo a cualquier implementación (2026-08-09) trazó el origen de la palabra "historial" en el criterio de A5 hasta el commit `d8d429d` (creación de esta checklist, 2026-07-25) — nunca modificada desde entonces. El único documento contemporáneo a esa redacción (`PROJECT.md`, sección de registro de la checklist) confirma que la ampliación de A5 hacia cambio de dispositivo fue "consecuencia natural de una arquitectura ya centrada en el servidor, no funcionalidad nueva". No existe en el repositorio ninguna tabla, función, pantalla ni concepto dedicado de "historial" de usuario, y no hay evidencia documental de qué se pretendía nombrar con esa palabra. Mantenerla exigiría construir una funcionalidad nueva —contradiciendo el principio fundacional de este documento, "No introduce funcionalidad nueva"— o forzar una equivalencia no respaldada (evaluada y descartada explícitamente: las reacciones `me_gusta`/`ya_fui` de `interactions` no están documentadas como "historial" en ninguna parte del proyecto). Se retira la palabra del criterio de A5; el resto del criterio permanece exactamente igual. Esta es una corrección documental de una inconsistencia en el propio texto de la checklist — no cambia ninguna funcionalidad de Ahorita, no reduce ni amplía el producto, y A5 permanece Pendiente. Ver el detalle completo de la trazabilidad en `PROJECT.md`.

## Nota — cierre de A5 (registro/login, cambio de dispositivo)

**A5 queda Hecho.** Con el alcance ya aclarado arriba (sin "historial"), las siete cláusulas del criterio quedaron verificadas con evidencia real: registro self-service, confirmación de correo, cierre/inicio de sesión sin intervención del equipo, segundo dispositivo, y conservación de guardados, seguimientos y preferencias — las tres últimas confirmadas cross-device (PC↔Android real) reabriendo la app en el segundo dispositivo sin repetir ninguna acción. La pieza que faltaba (seguimientos) se cerró reutilizando un lugar ya existente ("Parque Calderón") y un artefacto de prueba mínimo, ya limpiado tras obtener la evidencia. Ver el detalle completo, incluida la distinción explícita entre esta evidencia y la recuperación de contraseña del administrador (operativa, no parte del criterio de A5), en `PROJECT.md` ("A5 — CERRADO").

## Nota — cierre de A4 (Storage, RLS)

**A4 queda Hecho.** Bucket público `media`: subida real autenticada, lectura pública real sin sesión, y subida rechazada explícitamente por RLS cuando se intentó sin autenticación (mensaje de la propia API mencionando "row-level security policy") — las tres pruebas ejecutadas contra `ahorita-production` real, con limpieza posterior confirmada (el objeto de prueba dejó de responder tras eliminarse). Bucket privado `verification_evidence`: un objeto de prueba, subido manualmente vía Dashboard sin crear ninguna fila en `public.verifications` (mínimo impacto), confirmado existente antes del intento de acceso; una petición sin sesión contra el endpoint privado no lo recuperó (`NoSuchKey`/"Object not found"). **Precisión metodológica, no oculta:** ese mensaje no es una confirmación textual de "RLS" — es el comportamiento ya documentado en fuentes oficiales de Supabase (repositorio y foro de discusión de la organización `supabase`) de enmascarar denegaciones de acceso como inexistencia en buckets privados; la conclusión de cierre se apoya en la comparación controlada entre la existencia confirmada del objeto y su no recuperación bajo el mismo path sin sesión, no en una frase literal de error. Pruebas de propietario/administrador/tercero autenticado y la matriz completa de roles quedan explícitamente fuera de A4 — pertenecen a A6 y A21. Ver el detalle completo, incluidas las fuentes consultadas, en `PROJECT.md` ("A4 — CERRADO").

## Nota — cierre de A3 (secretos de producción)

**A3 queda Hecho**, con el alcance ya aclarado arriba. Verificado: sin secretos/tokens/credenciales hardcodeadas en el repositorio ni en su historial; el frontend desplegado realiza peticiones reales contra el project ref de `ahorita-production` (confirmado también en el dashboard de Supabase); Vercel Production contiene únicamente `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY`, ambas en el scope Production; la clave configurada tiene formato de JWT legacy, consistente con la clave `anon`/`public` que Supabase clasifica por separado de `service_role`/`secret` (nunca revelada ni copiada). **Limitación metodológica documentada con honestidad:** no se comparó byte por byte la JWT de Vercel contra la de Supabase, ni se decodificó su payload para confirmar el claim `role`, deliberadamente, para no manipular ni exponer innecesariamente la credencial completa — la conclusión se apoya en evidencia convergente (petición real exitosa contra el proyecto correcto; nomenclatura y clasificación coincidentes en código, Vercel y Supabase, tres superficies independientes), no en verificación criptográfica exhaustiva. Ver el detalle completo en `PROJECT.md` ("A3 — CERRADO").

## Nota — cierre de A2 (Vercel, PWA)

**A2 queda Hecho.** Frontend desplegado en Vercel (`https://ahorita-five.vercel.app`, HTTPS, rama de producción `claude/esto-tengo-2wzbnj` — temporal, ver `PROJECT.md` sobre la migración futura a `main`). Durante la validación se encontraron y corrigieron dos defectos reales (referencia a `supabase` faltante en `src/lib/feed.js`; ausencia de `self.skipWaiting()`/`clientsClaim()` en `src/sw.js`, causando que el service worker nuevo quedara indefinidamente en `waiting`) — ambos verificados como resueltos contra producción real, en escritorio y en Android real. El feed vacío observado durante las pruebas se investigó y se descartó como defecto: los 5 eventos de demostración habían vencido según su propia fecha real, no un fallo de `compose_feed()`.

**Limitación offline documentada explícitamente:** Ahorita conserva el shell de la PWA sin conexión (no colapsa, permite navegación básica), pero las vistas que dependen de datos remotos requieren conectividad — no hay caché de respuestas de Supabase. No se afirma funcionamiento offline completo.

**Dos matices metodológicos, documentados con honestidad, no ocultos:** la invalidación de caché y la ausencia de datos residuales tras logout se verificaron con inspección forense directa (DevTools) solo en escritorio; en Android se verificaron de forma funcional/de comportamiento (actualizaciones consecutivas correctas sin mezcla de versiones; sesión no restaurada tras cierre completo y reapertura), no mediante inspección remota de Cache Storage vía USB. Ver el detalle completo, la evidencia y el razonamiento en `PROJECT.md` ("A2 — CERRADO").

## Nota — cierre de A7 (trigger de creación de `profiles`)

**A7 queda Hecho.** Consulta de solo lectura contra `ahorita-production` (`auth.users` comparada con `public.profiles`) confirmó exactamente una fila de perfil por cada usuario real existente, sin huérfanos en ninguna dirección y sin duplicados: `total_auth_users = 4`, `total_profiles = 4`, cero usuarios sin perfil, cero perfiles sin usuario, cero `id` con más de una fila de perfil, con verificación puntual positiva adicional para dos cuentas reales (Pedro y Moisés). Ninguna escritura se ejecutó contra producción para obtener esta evidencia. Ver el detalle completo en `PROJECT.md` ("A7 — CERRADO").

## Nota — cierre de A16 (correo transaccional de producción)

**A16 queda Hecho.** Confirmación de registro y recuperación de contraseña verificadas con correos reales contra `ahorita-production`, cubriendo dos proveedores de correo distintos (no solo dos cuentas): un correo real de recuperación de contraseña recibido en Gmail, y un correo real de confirmación de una cuenta nueva registrada desde el frontend real de Ahorita con una dirección de Yahoo Mail. Ambos correos recientes muestran `redirect_to=https://ahorita-five.vercel.app`, sin `localhost`; remitente `Supabase Auth <noreply@mail.app.supabase.io>`, con SPF, DKIM (dominio `mail.app.supabase.io`) y DMARC en PASS, verificado directamente en Gmail → "Mostrar original". La confirmación de la cuenta de Yahoo se completó de punta a punta: enlace real, aceptación real, redirección real a `https://ahorita-five.vercel.app`, con el onboarding de intereses de una cuenta nueva cargando correctamente. **Hallazgo histórico, no ocultado:** un correo de confirmación real recibido el 7 de agosto mostraba `redirect_to=http://localhost:3000` — una configuración de `Site URL`/`Redirect URLs` incorrecta y real en ese momento. La configuración actual del Dashboard de Supabase (`Site URL: https://ahorita-five.vercel.app`, `Redirect URLs: https://ahorita-five.vercel.app/**`) ya no la reproduce, y el cierre de A16 no se apoya únicamente en esa configuración del Dashboard, sino en los dos correos reales nuevos, generados después de la corrección, que confirman empíricamente que el defecto quedó resuelto. Ver el detalle completo en `PROJECT.md` ("A16 — CERRADO").

## Nota — cierre de A20 (inventario de semillas)

**A20 queda Hecho.** Consulta de solo lectura contra `ahorita-production` verificó, uno a uno, cada semilla exigida por el criterio literal: administrador inicial (`profiles.is_admin=true`, 1 fila), actor de sistema "Ahorita Editorial" (`actors`, 1 fila), actor de sistema "Cuenta eliminada" (`actors`, 1 fila), ciudad "Cuenca" (`cities`, 1 fila), zonas "Centro Histórico" y "Turi" como hijas de Cuenca (`zones`, 1 fila cada una), y el catálogo completo de 10 canales (`channels`) sin ningún faltante. Ninguna escritura se ejecutó contra producción para obtener esta evidencia. Ver el detalle completo en `PROJECT.md` ("A20 — CERRADO").

## Nota — cierre de A19 (canal de soporte)

**A19 queda Hecho.** El canal de soporte de la beta (correo real de la persona responsable, visible en "Ajustes" desde el commit `05041a6`) recibió un mensaje real de prueba y obtuvo una respuesta real, con tiempo de respuesta registrado de aproximadamente 1 minuto — capturas reales conservadas del envío y de la respuesta. Consistente con el diseño explícito de esta etapa (`ETAPA_PRODUCTO_VIVO.md`: soporte como una persona respondiendo, no un sistema de tickets), sin construir ninguna funcionalidad nueva más allá del enlace de contacto ya cerrado en el código. Ver el detalle completo en `PROJECT.md` ("A19 — CERRADO").

## Nota — cierre de A13 (Términos y Privacidad), con desviación histórica documentada

**Estado actual, verificado con evidencia real:** el documento "Términos de Servicio y Política de Privacidad de Ahorita" existe, con contenido real (ceñido estrictamente a funcionalidades ya desplegadas — ver `PROJECT.md`), y está publicado en producción. `/legal` es una ruta pública, verificada en una ventana sin sesión (InPrivate) contra `https://ahorita-five.vercel.app/legal`, cargando correctamente. Existe enlace visible desde "Ajustes" (junto a "Ayuda y soporte", ya cerrado en A19) y desde `LoginPage.jsx`, visible antes de pulsar "Crear cuenta".

**Desviación histórica, no oculta:** el criterio literal de A13 exige que el documento estuviera "publicado antes del primer registro real". Eso **no ocurrió** — existieron registros reales (Moisés, Pedro, Ana, y las cuentas de prueba usadas durante A5/A6) antes de que este documento existiera. La publicación real ocurrió el **15 de agosto de 2026**. Este hecho es irreversible: no se reinterpreta el criterio, no se altera ninguna fecha, no se presenta como cumplido.

**Corrección prospectiva:** desde el 15 de agosto de 2026, cualquier registro **nuevo** sí encuentra el documento publicado y accesible antes de crear la cuenta — la condición queda corregida hacia adelante, aunque no pueda cumplirse retroactivamente para los registros que ya existían.

**Estado**: A13 queda **"Hecho, con desviación histórica documentada"** — no un "Hecho" limpio. Dos de las tres cláusulas del criterio (documento real, accesible desde la app) están demostradas con evidencia real de producción; la tercera (orden temporal respecto al primer registro) no se cumplió y no puede corregirse retroactivamente.

## Nota — ensayo real de rollback: frontend y `export-user-data` para A17 (en progreso, A17 permanece Pendiente)

Se ejecutó un ensayo real de rollback exclusivamente del frontend en Vercel: producción estaba en `1bfd3ed`; se promovió temporalmente el deployment de `05041a6` a Producción; se confirmó de forma observable (ausencia real del enlace "Términos de Servicio y Política de Privacidad" en `/ajustes`, que solo existe desde `1bfd3ed`) que el rollback surtió efecto real; se restauró inmediatamente `1bfd3ed` y se confirmó la reaparición del enlace. En ningún momento se tocó Supabase, la base de datos, RLS, migraciones ni datos de personas reales — el ensayo fue exclusivamente sobre el artefacto estático de Vercel. Capturas reales conservadas de ambos estados (durante el rollback y tras la restauración).

**Ensayo adicional, real, sobre una Edge Function (`export-user-data`):** desplegada y funcional (`VERSION 4`) → eliminada (`supabase functions delete`) → `functions list` confirmó su ausencia total → durante esa ausencia, un intento real de exportación desde la app no produjo una nueva exportación verificable → redesplegada (`supabase functions deploy`, identificador nuevo, `VERSION 1`) → `ACTIVE` → nueva exportación real posterior a la restauración (`generated_at = 2026-08-17T14:49:27.832Z`, restauración registrada el `2026-08-16` a las `23:47:34 UTC`) con la interacción real y conocida de Pedro intacta, más un `consent_record` de exportación también posterior a la restauración (`created_at = 2026-08-16T23:49:07.300664+00:00`). Un hallazgo inicial —un archivo JSON que se atribuyó por error a la ventana de ausencia— quedó corregido explícitamente: su `generated_at` (`2026-08-16T23:30:38.847Z`) demuestra que era anterior al `delete`, no evidencia de que la función respondiera durante su ausencia real. Ver el detalle completo en `PROJECT.md` ("A17 — ensayo real de rollback de Edge Function").

**Esto tampoco cierra A17.** Queda demostrada la reversibilidad real, de punta a punta, únicamente para `export-user-data` — la primera de las cinco Edge Functions con esta evidencia. Las otras cuatro (`ai-guide`, `send-push`, `process-account-deletions`, `process-verification-lifecycle`) siguen sin desplegarse contra `ahorita-production` y su reversibilidad sigue sin poder probarse — misma causa ya registrada en el cierre de A3 y en los ítems A8-A11/A16-bis. Ver el detalle completo en `PROJECT.md`.

## Nota — cierre de A10 (`export-user-data`)

**A10 queda Hecho.** Una primera exportación real (Pedro, cuenta ya usada en A5/A6) reveló un defecto real: `interactions` no aparecía en el JSON, aunque una consulta directa a producción confirmó `interactions_pedro = 1` — la Edge Function seguía consultando las tablas legacy (`saved_places`/`saved_events`/`follows`/`post_likes`), desactualizadas desde que la migración `0032` (Fase 5B) convirtió a `public.interactions` en la fuente de verdad real de guardados/reacciones. Corrección mínima aplicada en el commit `f0143b4`: se agregó una consulta a `interactions`, filtrada exclusivamente por el `actor_id` propio ya verificado (nunca un valor del cliente), manteniendo intactas las cuatro tablas legacy. Un primer despliegue (VERSION 3) resultó no reflejar el commit corregido — diagnosticado y confirmado como un desfase entre el checkout local usado para el `deploy` y el commit real; un segundo despliegue (VERSION 4) sí lo reflejó. Una nueva exportación real confirmó la interacción exacta de Pedro (`guardado`/`place`/`Parque Calderón`), sin ningún dato de otra cuenta. Ver el detalle completo en `PROJECT.md` ("A10 — CERRADO").

## B. Verificaciones obligatorias durante la primera semana de beta

| # | Objetivo | Criterio objetivo | Evidencia a conservar | Estado |
|---|---|---|---|---|
| B1 | Costo real por conversación medido | Cifra real de al menos una semana de uso piloto, comparada contra la estimación de `ETAPA_PRODUCTO_VIVO.md` §15 y contra el presupuesto máximo aceptado (A8) | Registro de costo real semanal | Pendiente |
| B2 | Instrumentación (A18) confirmada funcionando con datos reales | Los datos agregados se están capturando limpiamente con el primer grupo piloto real, sin fuga de contenido privado | Muestra de datos agregados reales revisada | Pendiente |
| B3 | Canal de soporte (A19) confirmado bajo uso real | Al menos una solicitud real de la primera semana fue atendida dentro del tiempo objetivo | Registro de la solicitud real y su tiempo de respuesta | Pendiente |

## C. Condiciones necesarias antes de ampliar el grupo piloto

| # | Objetivo | Criterio objetivo | Evidencia a conservar | Estado |
|---|---|---|---|---|
| C1 | Sostenibilidad económica confirmada | El costo real (B1) se mantiene dentro del presupuesto aceptado, o el mecanismo de límite (A8) se ajustó explícitamente | Decisión documentada | Pendiente |
| C2 | Sin fuga real de datos entre personas | Ni la matriz de RLS (A6) ni la instrumentación (A18/B2) revelaron una fuga real durante la primera semana | Confirmación explícita, sin incidentes abiertos | Pendiente |
| C3 | Respaldo probado en la práctica, si fue necesario | Si el respaldo (A14) tuvo que usarse en un incidente real, la restauración fue exitosa; si no se usó, sigue vigente la prueba de A14 | Registro del incidente y su resolución, si ocurrió | Pendiente |
| C4 | Soporte sostenible antes de sumar más personas | El canal de soporte (A19/B3) sostuvo su tiempo de respuesta objetivo con el primer grupo — señal directa de si el rol ampliado del fundador (`ETAPA_PRODUCTO_VIVO.md` §5/§21) es sostenible antes de crecer | Registro de tiempos de respuesta de la primera semana | Pendiente |

---

## Explícitamente fuera de esta checklist

Ninguno de los siguientes puntos se excluye por costumbre, por ser "genérico" o por parecer secundario — se excluye porque, hoy, no existe evidencia real de que su ausencia impida a una persona o negocio real usar Ahorita durante esta etapa. El criterio de inclusión de toda esta checklist es uniforme: se incorpora lo que la evidencia ya exige; se difiere lo que todavía no la tiene, y su eventual incorporación queda condicionada a que esa evidencia aparezca durante la beta (vía el canal de soporte, A19/B3, o la instrumentación, A18/B2).

- **Inicio de sesión con Google / Apple.** Sin evidencia de que el registro por correo genere fricción real para el tamaño de piloto que define `ETAPA_PRODUCTO_VIVO.md`. Se reevalúa si aparece evidencia real de abandono de registro o solicitud repetida.
- **Cambio de contraseña / cambio de correo desde la interfaz.** El canal de soporte (A19) resuelve hoy cualquier caso real. Se reevalúa si el volumen de estas solicitudes deja de ser manejable manualmente.
- **Distribución mediante Google Play / TestFlight.** Contradice el modelo de confianza personal de `ETAPA_PRODUCTO_VIVO.md` §5 (el fundador recluta y acompaña directamente); además, es trabajo de ingeniería nuevo, no verificación de algo ya construido. Pertenece a una futura etapa de crecimiento, no a esta.
- **CI/CD, observabilidad completa, cola de moderación, feature flags, internacionalización, preparación multi-ciudad, y cualquier funcionalidad nueva** (Historias/video, QR, comercio, monetización) — ya excluidos desde el análisis original de esta checklist.

---

*Fin del documento. Checklist operativa viva, subordinada a `ETAPA_PRODUCTO_VIVO.md`. Se actualiza directamente en este archivo conforme cada ítem se verifica con evidencia real — no se reescribe como documento distinto ni se convierte en canónico.*
